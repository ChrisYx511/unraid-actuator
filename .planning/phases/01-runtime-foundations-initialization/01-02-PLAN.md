---
phase: 01-runtime-foundations-initialization
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/unraid_actuator/cli.py
  - src/unraid_actuator/config.py
  - src/unraid_actuator/init.py
  - tests/unit/test_config_store.py
  - tests/unit/test_init_command.py
autonomous: true
requirements:
  - INIT-01
  - INIT-02
  - INIT-03
  - INIT-04
must_haves:
  truths:
    - Operator can run `unraid-actuator init` with repo URL, deploy branch, hostname, and managed source path.
    - A missing or empty managed source path is prepared for initialization, while an existing non-empty path is reused without recloning.
    - The single active config is persisted at `/tmp/actuator-cfg.yml` with exactly the Phase 1 fields.
  artifacts:
    - path: src/unraid_actuator/config.py
      provides: active-config dataclass, schema, and persistence helpers for `/tmp/actuator-cfg.yml`
      contains: /tmp/actuator-cfg.yml
    - path: src/unraid_actuator/init.py
      provides: init workflow orchestration for clone-or-reuse behavior
      contains: git clone
    - path: src/unraid_actuator/cli.py
      provides: wired `init` subcommand argument parsing
      contains: repo-url
    - path: tests/unit/test_init_command.py
      provides: proof for missing-dir create, reuse-without-reclone, and dry-run behavior
      contains: init
  key_links:
    - from: src/unraid_actuator/cli.py
      to: src/unraid_actuator/init.py
      via: parsed init arguments mapped into the init workflow
      pattern: source_path
    - from: src/unraid_actuator/init.py
      to: src/unraid_actuator/runner.py
      via: git clone command execution through the shared runner
      pattern: git clone
    - from: src/unraid_actuator/init.py
      to: src/unraid_actuator/config.py
      via: persisted active config write after clone/reuse
      pattern: actuator-cfg.yml
---

<objective>
Implement the Phase 1 `init` workflow so operators can configure one managed host checkout, persist the active settings contract, and exercise the command through the packaged CLI.

Purpose: Deliver the first real operator-facing command while preserving the strict Phase 1 boundary.
Output: Active-config schema/persistence helpers, init orchestration, CLI wiring, and unit tests for clone/reuse/config behavior.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-runtime-foundations-initialization/01-CONTEXT.md
@src/unraid_actuator/cli.py
@src/unraid_actuator/runner.py

<interfaces>
From `src/unraid_actuator/cli.py` (from Plan 01 output):
```python
def build_parser() -> "argparse.ArgumentParser": ...
def main(argv: "Sequence[str] | None" = None) -> int: ...
```

From `src/unraid_actuator/runner.py` (from Plan 01 output):
```python
class CommandRunner(Protocol):
    def run(self, spec: CommandSpec) -> CommandResult: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the persisted active-config contract</name>
  <files>src/unraid_actuator/config.py, tests/unit/test_config_store.py</files>
  <behavior>
    - Test 1: Saving an active config writes `/tmp/actuator-cfg.yml` with repo URL, deploy branch, hostname, and managed source path.
    - Test 2: Loading the config returns the same typed values through a strict schema.
    - Test 3: Missing or extra keys fail the schema instead of silently broadening the contract.
  </behavior>
  <action>Create `src/unraid_actuator/config.py` with an `ActiveConfig` dataclass, `ACTIVE_CONFIG_PATH = Path("/tmp/actuator-cfg.yml")`, and `strictyaml` schema-driven load/save helpers for the exact D-03 field set: repo URL, deploy branch, hostname, and managed source path. Keep this a single-active-config contract only — no profiles, history, validation/build metadata, or extra keys. Ensure writes create parent directories as needed and use explicit UTF-8 text handling.</action>
  <verify>
    <automated>uv run pytest tests/unit/test_config_store.py -q</automated>
  </verify>
  <done>The active config contract is explicit, schema-backed, persisted at `/tmp/actuator-cfg.yml`, and rejects contract drift.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement `init` clone-or-reuse orchestration and CLI wiring</name>
  <files>src/unraid_actuator/init.py, src/unraid_actuator/cli.py, tests/unit/test_init_command.py</files>
  <behavior>
    - Test 1: `init` with a missing source path creates the required directories, issues the expected `git clone`, and persists config.
    - Test 2: `init` with an existing non-empty source path skips recloning and still persists the active config.
    - Test 3: `init --dry-run` reports intended actions through the shared runner without mutating the filesystem or writing `/tmp/actuator-cfg.yml`.
  </behavior>
  <action>Implement `src/unraid_actuator/init.py` as the Phase 1 orchestration layer and wire it into the existing thin argparse shell in `src/unraid_actuator/cli.py` per D-02. Accept the required Phase 1 flags for INIT-01: `--repo-url`, `--deploy-branch`, `--hostname`, and `--source-path`, then pass the parsed dry-run intent to the shared runner per D-04. In non-dry-run mode, create parent/source directories when missing (INIT-02), run `git clone --branch {deploy_branch} --single-branch {repo_url} {source_path}` only when the target directory is missing or empty, and explicitly skip recloning when the directory already contains files (INIT-03). Persist the exact active config via `config.py` after successful clone-or-reuse (INIT-04). Preserve the phase boundary: do not add repository validation beyond empty/non-empty handling, and do not implement validate/build/deploy/reconcile logic here.</action>
  <verify>
    <automated>uv run pytest tests/unit/test_init_command.py -q</automated>
  </verify>
  <done>`unraid-actuator init` supports the required arguments, creates missing paths, reuses non-empty source directories without recloning, and writes the single active config contract outside dry-run mode.</done>
</task>

</tasks>

<verification>
Run the targeted config and init tests, then manually spot-check `uv run python -m unraid_actuator init --help` to confirm the CLI presents the required arguments without surfacing future-phase commands.
</verification>

<success_criteria>
- `unraid-actuator init` accepts repo URL, deploy branch, hostname, and managed source path.
- Missing paths are created automatically before clone.
- Existing non-empty source paths are reused without recloning.
- `/tmp/actuator-cfg.yml` stores exactly one active config with the Phase 1 fields.
</success_criteria>

<output>
After completion, create `.planning/phases/01-runtime-foundations-initialization/01-02-SUMMARY.md`
</output>
