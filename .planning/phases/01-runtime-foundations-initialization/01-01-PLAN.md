---
phase: 01-runtime-foundations-initialization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - main.py
  - src/unraid_actuator/__init__.py
  - src/unraid_actuator/__main__.py
  - src/unraid_actuator/cli.py
  - src/unraid_actuator/runner.py
  - tests/conftest.py
  - tests/unit/test_cli_smoke.py
  - tests/unit/test_runner.py
autonomous: true
requirements:
  - PKG-01
  - PKG-02
  - PKG-03
  - OPS-04
must_haves:
  truths:
    - Developer can run `uv build` and produce a distributable package.
    - Developer can import `unraid_actuator` and invoke the packaged CLI entrypoint after installation.
    - External command execution can be simulated in tests without requiring live git, docker, or ejson binaries.
  artifacts:
    - path: pyproject.toml
      provides: uv_build backend, console entrypoint, runtime dependencies, and pytest configuration
      contains: build-backend = "uv_build"
    - path: src/unraid_actuator/cli.py
      provides: thin argparse CLI shell with a public --dry-run surface
      contains: argparse
    - path: src/unraid_actuator/runner.py
      provides: shared command runner abstraction and dry-run-aware implementation
      contains: dry_run
    - path: tests/unit/test_runner.py
      provides: unit proof that command execution can be simulated without live binaries
      contains: dry_run
  key_links:
    - from: pyproject.toml
      to: src/unraid_actuator/__main__.py
      via: console script entrypoint
      pattern: unraid-actuator
    - from: src/unraid_actuator/cli.py
      to: src/unraid_actuator/runner.py
      via: parsed --dry-run flag passed into runner selection
      pattern: dry_run
    - from: main.py
      to: src/unraid_actuator/cli.py
      via: compatibility shim
      pattern: unraid_actuator
---

<objective>
Create the installable Phase 1 package skeleton so the project builds with `uv_build`, exposes a thin argparse CLI, and provides a reusable dry-run-friendly command runner.

Purpose: Lock the packaging and execution contracts before any init workflow logic lands.
Output: A `src/` package, package entrypoints, runner abstractions, and smoke/unit tests for build/import/dry-run behavior.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-runtime-foundations-initialization/01-CONTEXT.md
@pyproject.toml
@main.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Establish the `uv_build` package scaffold and public CLI entrypoints</name>
  <files>pyproject.toml, main.py, src/unraid_actuator/__init__.py, src/unraid_actuator/__main__.py, src/unraid_actuator/cli.py</files>
  <behavior>
    - Test 1: `uv build` succeeds and emits a distributable package artifact.
    - Test 2: `python -m unraid_actuator --help` renders argparse help containing the `init` command and `--dry-run`.
    - Test 3: `import unraid_actuator` succeeds from the installed package surface.
  </behavior>
  <action>Update `pyproject.toml` for Python 3.13+ only per D-01, add a real `[build-system]` using `uv_build`, define the `src/` package layout, declare the console entrypoint `unraid-actuator`, add the minimal runtime dependency needed for YAML config work (`strictyaml`), and add pytest dev/test configuration for PKG-01/PKG-02. Create `src/unraid_actuator/` with `__init__.py` exports, `__main__.py` delegation, and a thin stdlib `argparse` shell in `cli.py` per D-02. Keep the shell intentionally thin and Phase-1-scoped: expose `init` plus the public `--dry-run` contract, but do not implement validation/build/deploy/reconcile behavior here. Replace root `main.py` with a compatibility shim that calls the packaged CLI entrypoint instead of keeping standalone logic.</action>
  <verify>
    <automated>uv build &amp;&amp; uv run python -m unraid_actuator --help</automated>
  </verify>
  <done>`uv build` works, the package is importable from `src/`, `unraid-actuator` resolves to packaged code, and the CLI shell advertises `init` plus `--dry-run`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the shared dry-run-friendly command runner and package smoke tests</name>
  <files>src/unraid_actuator/runner.py, tests/conftest.py, tests/unit/test_cli_smoke.py, tests/unit/test_runner.py</files>
  <behavior>
    - Test 1: A dry-run runner returns a structured result without executing the external command.
    - Test 2: A fake/recording runner captures argv, cwd, env, and execution intent in the same result shape used by real execution.
    - Test 3: CLI smoke tests prove the parser accepts `--dry-run` without needing live binaries.
  </behavior>
  <action>Create `src/unraid_actuator/runner.py` with typed command-spec/result dataclasses plus a runner protocol used by later services. Include a real subprocess-backed implementation and a dry-run/recording implementation that captures argv, cwd, env overrides, stdout/stderr placeholders, exit status, and whether execution was skipped per D-04. Keep this abstraction generic — no git/docker-specific branching yet. Add pytest fixtures and focused unit tests proving commands can be inspected or simulated without requiring installed git/docker/ejson binaries, satisfying OPS-04 and PKG-03.</action>
  <verify>
    <automated>uv run pytest tests/unit/test_cli_smoke.py tests/unit/test_runner.py -q</automated>
  </verify>
  <done>The codebase has one shared runner contract for future shell-outs, dry-run execution is observable in tests, and package/CLI smoke tests pass without external tools.</done>
</task>

</tasks>

<verification>
Run `uv build` once, then run the targeted pytest files for CLI and runner behavior. Confirm no test depends on live git, docker, or ejson binaries.
</verification>

<success_criteria>
- `uv build` completes successfully.
- `unraid_actuator` is importable from the packaged `src/` layout.
- The CLI shell is thin `argparse` code with a visible `--dry-run` contract.
- A shared runner abstraction exists and can simulate commands in tests.
</success_criteria>

<output>
After completion, create `.planning/phases/01-runtime-foundations-initialization/01-01-SUMMARY.md`
</output>
