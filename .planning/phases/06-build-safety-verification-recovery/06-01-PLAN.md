---
phase: 06-build-safety-verification-recovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/unraid_actuator/build.py
  - src/unraid_actuator/cli.py
  - tests/unit/test_build_service.py
  - tests/unit/test_build_cli.py
autonomous: true
requirements:
  - BLD-01
  - BLD-02
  - BLD-03
  - BLD-04
  - BLD-05
  - BLD-06
  - BLD-07
gap_closure: true
must_haves:
  truths:
    - "Operators cannot start secret decryption or staged runtime-tree materialization from `unraid-actuator build` when the host tree fails validation."
    - "A valid host tree still builds through the existing build service and CLI entrypoint without changing the successful output-root contract."
    - "Validation-blocked builds surface as a readable build failure at the CLI boundary instead of partially building."
  artifacts:
    - path: "src/unraid_actuator/build.py"
      provides: "Build-service validation gate enforced before decrypt/materialization"
      contains: "run_validate_for_host"
    - path: "tests/unit/test_build_service.py"
      provides: "Regression coverage for pre-materialization validation blocking"
      contains: "validation"
    - path: "tests/unit/test_build_cli.py"
      provides: "CLI coverage for validation-blocked build failures"
      contains: "build"
  key_links:
    - from: "src/unraid_actuator/build.py"
      to: "src/unraid_actuator/validate.py"
      via: "Build orchestration must call `run_validate_for_host(...)` before output-root staging or secret decryption"
      pattern: "run_validate_for_host"
    - from: "src/unraid_actuator/cli.py"
      to: "src/unraid_actuator/build.py"
      via: "CLI `build` command must keep surfacing service `ValueError` failures as exit code 1"
      pattern: "run_build"
---

<objective>
Close GAP-01 in shipped code by enforcing validate-before-build at the build-service boundary and locking the public CLI behavior around that safety gate.

Purpose: Re-establish the intended validate -> build flow so invalid host trees cannot reach decrypt/materialization work.
Output: Updated build-service orchestration plus unit coverage proving the enforced gate and CLI failure surface.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/06-build-safety-verification-recovery/06-CONTEXT.md
@.planning/phases/06-build-safety-verification-recovery/06-DISCUSSION-LOG.md
@.planning/phases/03-runtime-build-secret-materialization/03-04-PLAN.md
@.planning/phases/03-runtime-build-secret-materialization/03-05-PLAN.md
@src/unraid_actuator/build.py
@src/unraid_actuator/validate.py
@src/unraid_actuator/cli.py
@tests/unit/test_build_service.py
@tests/unit/test_build_cli.py
@tests/unit/test_validate_service.py

<interfaces>
From `src/unraid_actuator/build.py`:
```python
def run_build(
    *,
    runner: CommandRunner,
    config_path: Path = ACTIVE_CONFIG_PATH,
    output_root: Path | None = None,
) -> BuildResult: ...

def run_build_for_host(
    *,
    runner: CommandRunner,
    host_root: Path,
    output_root: Path | None = None,
) -> BuildResult: ...
```

From `src/unraid_actuator/validate.py`:
```python
def run_validate_for_host(
    *,
    runner: CommandRunner,
    host_root: Path,
    app: str | None = None,
    environment: str | None = None,
) -> ValidationReport: ...
```

From `src/unraid_actuator/validation_models.py`:
```python
@dataclass(frozen=True)
class ValidationReport:
    findings: tuple[ValidationFinding, ...]
    checked_targets: tuple[DeclaredEnvironment, ...]

    @property
    def errors(self) -> tuple[ValidationFinding, ...]: ...

    @property
    def has_errors(self) -> bool: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce validate-before-build inside the build service</name>
  <files>src/unraid_actuator/build.py, tests/unit/test_build_service.py</files>
  <behavior>
    - Test 1: `run_build(...)` / `run_build_for_host(...)` call `run_validate_for_host(...)` before `resolve_output_root(...)`, `validate_output_root(...)`, `create_stage_root(...)`, `decrypt_secret_env(...)`, or any runtime-tree writes.
    - Test 2: when validation returns one or more errors, build raises a readable `ValueError` and no decrypt/materialization work occurs.
    - Test 3: validation warnings remain non-blocking, and an otherwise valid host still produces the same `BuildResult`/marker/output-root behavior as today.
  </behavior>
  <action>Update `src/unraid_actuator/build.py` so the build workflow explicitly depends on `run_validate_for_host(...)` per D-02 and the milestone-audit `GAP-01` finding. Call validation immediately after `host_root` resolution and before any output-root validation, stage-root creation, secret decryption, compose normalization, or `.env`/marker writes. Do not duplicate validation rules in build; consume the shared validation service as the single gate. If `report.has_errors` is true, raise `ValueError` with a concise operator-facing summary that makes clear build was blocked by validation and names the failing app/environment or host-level finding codes; warnings must not block build. Preserve the existing declared-target filtering, stage-then-promote behavior, dry-run behavior, and successful `BuildResult` contract once validation passes. Extend `tests/unit/test_build_service.py` to prove the validation call happens before any materialization helpers, that validation errors prevent decrypt/stage work, and that successful builds still complete after a clean validation report.</action>
  <verify>
    <automated>uv run pytest tests/unit/test_build_service.py -q</automated>
  </verify>
  <done>`run_build` cannot begin materialization for an invalid host tree, and service tests prove the validation gate happens before decrypt/stage side effects.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock the CLI regression surface around validation-blocked builds</name>
  <files>src/unraid_actuator/cli.py, tests/unit/test_build_cli.py</files>
  <behavior>
    - Test 1: the existing `build` CLI success path still passes the configured output path into `run_build(...)`.
    - Test 2: a validation-blocked build failure prints the service message to stderr and returns exit code `1`.
    - Test 3: no new host-only flags, UAT prompts, or Phase 7 git-failure handling are added here.
  </behavior>
  <action>Keep the CLI thin per the shipped Phase 3 contract: only adjust `src/unraid_actuator/cli.py` if needed to preserve the safe failure surface after Task 1. Add or update `tests/unit/test_build_cli.py` so the public `build` command explicitly covers the new validation-blocked failure path while preserving existing default/custom output-root dispatch. Per D-01, do not add host-only verification hooks or UAT steps here. Per the phase boundary, do not absorb unrelated git `RuntimeError` normalization that belongs to Phase 7.</action>
  <verify>
    <automated>uv run pytest tests/unit/test_build_cli.py -q</automated>
  </verify>
  <done>The CLI continues to expose one thin `build` command, and validation-blocked builds now fail cleanly with exit code `1` and a readable error message.</done>
</task>

</tasks>

<verification>
Run `uv run pytest tests/unit/test_build_service.py tests/unit/test_build_cli.py tests/unit/test_validate_service.py -q` to confirm the build-service gate and CLI regression coverage stay green together.
</verification>

<success_criteria>
- `unraid-actuator build` cannot reach secret decryption or stage-root creation when validation has errors.
- Valid builds still return a `BuildResult` and success message without changing the output-root contract.
- GAP-01 is closed in code, not just by operator sequencing.
</success_criteria>

<output>
After completion, create `.planning/phases/06-build-safety-verification-recovery/06-01-SUMMARY.md`
</output>
