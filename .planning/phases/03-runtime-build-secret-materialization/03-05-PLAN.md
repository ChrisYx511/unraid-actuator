---
phase: 03-runtime-build-secret-materialization
plan: 05
type: execute
wave: 4
depends_on:
  - 03-02
  - 03-04
files_modified:
  - src/unraid_actuator/compose_validation.py
  - src/unraid_actuator/validate.py
  - src/unraid_actuator/cli.py
  - tests/unit/test_compose_validation.py
  - tests/unit/test_validate_service.py
  - tests/unit/test_build_cli.py
autonomous: true
requirements:
  - BLD-01
  - BLD-04
  - BLD-05
  - BLD-07
must_haves:
  truths:
    - "Operators see the same source-rule outcome whether they validate a host or build it, including when host declarations are named `apps.yaml` or `apps.yml`."
    - "Operators can run `unraid-actuator build` and get either a successful output-root message or a safe failure."
    - "Operators can use the public CLI build flow without any repo-provided Python generation step."
  artifacts:
    - path: src/unraid_actuator/compose_validation.py
      provides: "Template-based validation preflight helpers aligned to Phase 3 source rules"
      contains: "template"
    - path: src/unraid_actuator/cli.py
      provides: "Operator-facing `build` subcommand"
      contains: "\"build\""
    - path: tests/unit/test_build_cli.py
      provides: "Proof of build CLI argument handling and exit behavior"
      contains: "test_build"
  key_links:
    - from: src/unraid_actuator/validate.py
      to: src/unraid_actuator/compose_validation.py
      via: "Validation preflight must call the same template-aware normalization path as build"
      pattern: "validate_template"
    - from: src/unraid_actuator/validate.py
      to: src/unraid_actuator/schemas.py
      via: "Validation must resolve host declarations through the shared `apps.y[a]ml` helper before it decides what the active host declares"
      pattern: "resolve_host_apps_path"
    - from: src/unraid_actuator/cli.py
      to: src/unraid_actuator/build.py
      via: "The new subcommand should be thin argparse wiring into `run_build(...)`"
      pattern: "run_build"
---

<objective>
Expose the Phase 3 build command at the CLI boundary and remove the remaining validation/preflight drift so template-based environments are treated consistently everywhere.

Purpose: Finish Phase 3 as an operator-facing feature and ensure validation/build share the same declarative template contract.
Output: Template-aware validation preflight updates, `build` CLI wiring, and unit tests for command-spec alignment plus CLI exit behavior.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-runtime-build-secret-materialization/03-CONTEXT.md
@.planning/phases/03-runtime-build-secret-materialization/03-RESEARCH.md
@.planning/phases/03-runtime-build-secret-materialization/03-VALIDATION.md
@src/unraid_actuator/cli.py
@src/unraid_actuator/compose_validation.py
@src/unraid_actuator/validate.py
@src/unraid_actuator/schemas.py
@src/unraid_actuator/build.py
@src/unraid_actuator/template_render.py
@src/unraid_actuator/compose_build.py

<interfaces>
Use the build service contract from 03-04:
```python
@dataclass(frozen=True)
class BuildResult:
    output_root: Path
    built_targets: tuple[BuildTarget, ...]

def run_build(
    *,
    runner: CommandRunner,
    config_path: Path = ACTIVE_CONFIG_PATH,
    output_root: Path | None = None,
) -> BuildResult: ...

def resolve_host_apps_path(host_root: Path) -> Path: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Align validation preflight with declarative template rendering</name>
  <files>src/unraid_actuator/compose_validation.py, src/unraid_actuator/validate.py, tests/unit/test_compose_validation.py, tests/unit/test_validate_service.py</files>
  <read_first>
    - .planning/phases/03-runtime-build-secret-materialization/03-CONTEXT.md
    - .planning/phases/03-runtime-build-secret-materialization/03-RESEARCH.md
    - src/unraid_actuator/compose_validation.py
    - src/unraid_actuator/validate.py
    - src/unraid_actuator/schemas.py
    - src/unraid_actuator/template_render.py
    - src/unraid_actuator/compose_build.py
    - src/unraid_actuator/discovery.py
    - tests/unit/test_compose_validation.py
    - tests/unit/test_validate_service.py
  </read_first>
  <behavior>
    - Test 1: template-based environments are preflighted by rendering `template.y[a]ml` plus `values.y[a]ml`, not by executing repo `build.py`.
    - Test 2: validation remains secret-free and does not decrypt or inject secrets into template rendering.
    - Test 3: grouped validation still preserves full-host collision context while using the new `SourceKind.TEMPLATE` flow and the shared `apps.yaml` / `apps.yml` resolver.
  </behavior>
  <action>Update `src/unraid_actuator/compose_validation.py` and `src/unraid_actuator/validate.py` so validation no longer references repo `build.py`. Replace the old dynamic-build helper with `validate_template_source(candidate: DiscoveredEnvironment, project_name: str, *, runner: CommandRunner) -> tuple[ValidationFinding, ...]` that loads `candidate.template_file` and `candidate.values_file`, renders through `render_template_environment(...)`, then normalizes with the same Compose helper logic from `compose_build.py`. Keep this validation path secret-free per D-05 and D-07: it may read only mapping-only values YAML and must not decrypt `secret-env.ejson` or read repo `.env`. Update `run_validate(...)` and any host-declaration loading path in `validate.py` so they resolve declarations through the shared `resolve_host_apps_path(host_root)` helper from 03-01 instead of assuming `apps.yaml`; `_preflight_findings(...)` must branch on `SourceKind.TEMPLATE` instead of `BUILD_PY`, and operator-facing messages/tests must name `template.yml` / `template.yaml`, `values.yml` / `values.yaml`, and `apps.yml` / `apps.yaml` support per D-01, D-02, and D-11. Add or update `tests/unit/test_compose_validation.py` and `tests/unit/test_validate_service.py` to prove the new template-based preflight route, shared `apps.y[a]ml` resolution, and full-host collision context still hold.</action>
  <acceptance_criteria>
    - `grep -q 'validate_template_source' src/unraid_actuator/compose_validation.py`
    - `grep -q 'SourceKind.TEMPLATE' src/unraid_actuator/validate.py`
    - `grep -q 'resolve_host_apps_path' src/unraid_actuator/validate.py`
    - `uv run pytest tests/unit/test_compose_validation.py tests/unit/test_validate_service.py -q` passes
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/unit/test_compose_validation.py tests/unit/test_validate_service.py -q</automated>
  </verify>
  <done>Validation and build now share the same declarative template source and `apps.y[a]ml` host-declaration contract, without reintroducing secret use or repo-side Python execution during preflight.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the thin `build` CLI subcommand and safe exit behavior</name>
  <files>src/unraid_actuator/cli.py, tests/unit/test_build_cli.py</files>
  <read_first>
    - .planning/phases/03-runtime-build-secret-materialization/03-CONTEXT.md
    - .planning/phases/03-runtime-build-secret-materialization/03-VALIDATION.md
    - src/unraid_actuator/cli.py
    - src/unraid_actuator/build.py
    - src/unraid_actuator/build_models.py
    - tests/unit/test_validate_cli.py
  </read_first>
  <behavior>
    - Test 1: `unraid-actuator build` calls `run_build(...)` with the active config path and default output root behavior.
    - Test 2: `unraid-actuator build --output-path /custom/path` passes the exact custom path into the service.
    - Test 3: build failures print a readable error to stderr and return exit code `1`.
  </behavior>
  <action>Update `src/unraid_actuator/cli.py` to add a `build` subcommand while preserving the thin argparse shell from Phase 1. Add one optional flag: `--output-path`, parsed as a string path and converted with `Path(args.output_path)` before dispatch. Route the command to `run_build(runner=selected_runner, config_path=config_path, output_root=...)` using the same injected runner pattern as `init` and `validate`. On success, print `Built runtime tree at {result.output_root}` and return `0`. Catch `YAMLValidationError` plus `ValueError` from build-root, render, compose, or secret failures; print the original message to `stderr` and return `1` so BLD-01, BLD-05, and BLD-07 surface safely at the operator boundary. Add `tests/unit/test_build_cli.py` that monkeypatches `run_build`, verifies default/custom output-path dispatch, and verifies exit-code/error-print behavior. Do not add scope flags or deploy behavior in this phase.</action>
  <acceptance_criteria>
    - `grep -q 'add_parser(\"build\"' src/unraid_actuator/cli.py`
    - `grep -q -- '--output-path' src/unraid_actuator/cli.py`
    - `uv run pytest tests/unit/test_build_cli.py -q` passes
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/unit/test_build_cli.py -q</automated>
  </verify>
  <done>The operator-facing CLI now exposes the Phase 3 build workflow with a thin `build` subcommand, safe error handling, and optional custom output-root selection.</done>
</task>

</tasks>

<verification>
Run `uv run pytest tests/unit/test_compose_validation.py tests/unit/test_validate_service.py tests/unit/test_build_cli.py -q`, then run `uv run python -m unraid_actuator build --help` to confirm the public CLI exposes `--output-path` without changing the existing `init` or `validate` surfaces.
</verification>

<success_criteria>
- Validation no longer assumes repo-side `build.py`.
- `unraid-actuator build` is publicly available and thinly wired into the build service.
- Phase 3 finishes with one consistent declarative source contract across discovery, validation, and build.
</success_criteria>

<output>
After completion, create `.planning/phases/03-runtime-build-secret-materialization/03-05-SUMMARY.md`
</output>
