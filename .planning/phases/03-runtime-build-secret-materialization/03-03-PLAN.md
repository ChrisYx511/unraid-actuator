---
phase: 03-runtime-build-secret-materialization
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/unraid_actuator/secrets.py
  - src/unraid_actuator/env_materialize.py
  - tests/unit/test_secrets.py
  - tests/unit/test_env_materialize.py
autonomous: true
requirements:
  - BLD-05
  - BLD-06
must_haves:
  truths:
    - "Secret decryption failures stop the build before any final runtime tree swap."
    - "A missing app/environment secret block is treated as an empty secret set, not as a failure."
    - "Merged `.env` output gives decrypted secret keys precedence over non-secret `.env` values."
  artifacts:
    - path: src/unraid_actuator/secrets.py
      provides: "EJSON decrypt adapter and per-environment secret extraction"
      contains: "ejson"
    - path: src/unraid_actuator/env_materialize.py
      provides: "Dotenv parsing, merge precedence, and deterministic serialization"
      contains: "dotenv_values"
    - path: tests/unit/test_env_materialize.py
      provides: "Proof of secret override precedence and deterministic `.env` output"
      contains: "override"
  key_links:
    - from: src/unraid_actuator/secrets.py
      to: src/unraid_actuator/runner.py
      via: "Secret decryption must stay behind the shared external-command abstraction"
      pattern: "CommandSpec"
    - from: src/unraid_actuator/env_materialize.py
      to: src/unraid_actuator/secrets.py
      via: "Materialized `.env` content merges repo values first, then per-environment secret values"
      pattern: "merge"
---

<objective>
Add the secret decryption and deterministic `.env` materialization helpers that keep secrets out of templates while producing the final environment file every built target needs.

Purpose: Establish the Phase 3 secret boundary clearly—decrypt once through EJSON, keep secrets out of Jinja, and emit a merged `.env` file with deterministic precedence and serialization.
Output: Secret adapter, `.env` merge/serialization helpers, and unit tests for decrypt failure, empty secret blocks, and override behavior.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-runtime-build-secret-materialization/03-CONTEXT.md
@.planning/phases/03-runtime-build-secret-materialization/03-RESEARCH.md
@.planning/phases/03-runtime-build-secret-materialization/03-VALIDATION.md
@src/unraid_actuator/runner.py
@src/unraid_actuator/schemas.py

<interfaces>
Reuse the existing secret metadata contract:
```python
def validate_secret_env_structure(path: Path) -> None: ...
```

Phase 3 secret helpers should expose environment-scoped data only:
```python
def decrypt_secret_env(*, host_root: Path, runner: CommandRunner) -> dict[str, object]: ...
def extract_environment_secrets(payload: dict[str, object], *, app: str, environment: str) -> dict[str, str]: ...
def materialize_env_file(*, env_file: Path | None, secrets: Mapping[str, str]) -> str: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the EJSON decrypt adapter and per-environment secret extraction</name>
  <files>src/unraid_actuator/secrets.py, tests/unit/test_secrets.py</files>
  <read_first>
    - .planning/phases/03-runtime-build-secret-materialization/03-CONTEXT.md
    - .planning/phases/03-runtime-build-secret-materialization/03-RESEARCH.md
    - src/unraid_actuator/runner.py
    - src/unraid_actuator/schemas.py
    - tests/unit/test_schemas.py
  </read_first>
  <behavior>
    - Test 1: secret decryption runs `ejson decrypt secret-env.ejson` through the shared runner and raises a hard error on non-zero exit.
    - Test 2: malformed decrypted JSON raises a hard error before any environment extraction proceeds.
    - Test 3: extracting secrets for a declared app/environment with no matching block returns `{}` instead of failing.
  </behavior>
  <action>Create `src/unraid_actuator/secrets.py` for D-07 through D-09 and BLD-06. Add `decrypt_secret_env(*, host_root: Path, runner: CommandRunner) -> dict[str, object]` that first calls the existing structural validator on `host_root / \"secret-env.ejson\"`, then runs `CommandSpec(argv=(\"ejson\", \"decrypt\", \"secret-env.ejson\"), cwd=host_root, env={}, inherit_env=True)`. On any non-zero exit, raise `ValueError(\"failed to decrypt secret-env.ejson: ...\")` using stderr/stdout; on invalid JSON, raise `ValueError(\"decrypted secret-env.ejson is not valid JSON: ...\")`. Add `extract_environment_secrets(payload, *, app: str, environment: str) -> dict[str, str]` that returns only non-underscore keys for the requested nested `app -> environment` block, validates string values, and returns `{}` when either the app key or environment key is absent per D-09. Do not pass decrypted secrets into template rendering helpers and do not log secret values in error text. Add `tests/unit/test_secrets.py` proving command construction, malformed decrypt output failure, and empty-secret-block behavior.</action>
  <acceptance_criteria>
    - `grep -q 'ejson' src/unraid_actuator/secrets.py`
    - `grep -q 'extract_environment_secrets' src/unraid_actuator/secrets.py`
    - `uv run pytest tests/unit/test_secrets.py -q` passes
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/unit/test_secrets.py -q</automated>
  </verify>
  <done>The project can decrypt host secrets through the runner abstraction, fail cleanly on decrypt problems, and treat missing environment secret blocks as empty inputs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Parse, merge, and serialize `.env` output with secret precedence</name>
  <files>src/unraid_actuator/env_materialize.py, tests/unit/test_env_materialize.py</files>
  <read_first>
    - .planning/phases/03-runtime-build-secret-materialization/03-CONTEXT.md
    - .planning/phases/03-runtime-build-secret-materialization/03-RESEARCH.md
    - pyproject.toml
    - src/unraid_actuator/secrets.py
  </read_first>
  <behavior>
    - Test 1: repo `.env` values load without mutating process environment.
    - Test 2: duplicate keys are overridden by secret values in the final materialized output.
    - Test 3: the serialized `.env` output is deterministic and rejects bare keys that parse as `None`.
  </behavior>
  <action>Create `src/unraid_actuator/env_materialize.py` for D-07 and D-08. Add `materialize_env_file(*, env_file: Path | None, secrets: Mapping[str, str]) -> str` that uses `dotenv_values(env_file, interpolate=False)` when a repo `.env` file exists, or `{}` when it does not. If any parsed repo value is `None`, raise `ValueError(f\".env entry '{key}' must include an explicit value\")` so ambiguous bare keys do not reach the runtime tree. Merge base env keys first and then `secrets` last so decrypted values override duplicates per D-08. Serialize deterministically by sorting keys ascending and emitting one `KEY=value` line per key; use the raw value when it matches `^[A-Za-z0-9_./:-]+$`, otherwise emit `KEY=` followed by `json.dumps(value)` so spaces, `#`, quotes, and newlines remain parseable. Always end the output with a trailing newline, and keep this helper focused on `.env` output only—never return template context data. Add `tests/unit/test_env_materialize.py` covering missing env files, secret override precedence, `None`-value rejection, and stable sorted serialization.</action>
  <acceptance_criteria>
    - `grep -q 'dotenv_values' src/unraid_actuator/env_materialize.py`
    - `grep -q 'json.dumps' src/unraid_actuator/env_materialize.py`
    - `uv run pytest tests/unit/test_env_materialize.py -q` passes
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/unit/test_env_materialize.py -q</automated>
  </verify>
  <done>The codebase can now produce one deterministic merged `.env` string per environment, with decrypted secrets overriding non-secret repo values and without leaking secrets into template inputs.</done>
</task>

</tasks>

<verification>
Run `uv run pytest tests/unit/test_secrets.py tests/unit/test_env_materialize.py -q`, then inspect `src/unraid_actuator/secrets.py` to confirm decrypt failures raise errors without embedding decrypted secret values in messages.
</verification>

<success_criteria>
- Secret decryption is isolated behind the runner abstraction and fails safely.
- Missing secret blocks do not break declared environments.
- `.env` output is deterministic and secret-overridden.
</success_criteria>

<output>
After completion, create `.planning/phases/03-runtime-build-secret-materialization/03-03-SUMMARY.md`
</output>
