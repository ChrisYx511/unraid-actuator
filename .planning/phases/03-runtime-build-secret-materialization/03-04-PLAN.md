---
phase: 03-runtime-build-secret-materialization
plan: 04
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
  - 03-03
files_modified:
  - src/unraid_actuator/build.py
  - src/unraid_actuator/build_models.py
  - tests/unit/test_build_service.py
autonomous: true
requirements:
  - BLD-01
  - BLD-02
  - BLD-03
  - BLD-05
  - BLD-06
  - BLD-07
must_haves:
  truths:
    - "A successful build creates a complete staged runtime tree and only then swaps it into place."
    - "The default build path is `/tmp/unraid-actuator/build`, and custom paths honor the empty-directory safety rule."
    - "Every successful build tree is marked with `.UNRAID_RUNNING_CONFIGURATION` at its root."
  artifacts:
    - path: src/unraid_actuator/build_models.py
      provides: "Typed build result contracts for the service and CLI"
      contains: "BuildResult"
    - path: src/unraid_actuator/build.py
      provides: "End-to-end build orchestration from active config to staged runtime tree"
      contains: "run_build"
    - path: tests/unit/test_build_service.py
      provides: "Proof of staging, marker, default path, and safe-failure behavior"
      contains: "marker"
  key_links:
    - from: src/unraid_actuator/build.py
      to: src/unraid_actuator/build_paths.py
      via: "Build orchestration must resolve/validate the final root and create a sibling stage root before writing files"
      pattern: "create_stage_root"
    - from: src/unraid_actuator/build.py
      to: src/unraid_actuator/template_render.py
      via: "Template environments render before Compose normalization"
      pattern: "render_template_environment"
    - from: src/unraid_actuator/build.py
      to: src/unraid_actuator/env_materialize.py
      via: "Materialized `.env` text is written beside normalized compose output"
      pattern: "materialize_env_file"
---

<objective>
Implement the staged build service that turns the configured host state into one actuator-managed runtime tree with normalized compose files, merged `.env` files, and a success marker.

Purpose: Deliver the actual Phase 3 outcome—an ephemeral, deterministic runtime tree that later deploy/reconcile phases can trust.
Output: Build DTOs, build orchestration service, and unit tests for stage-then-swap, marker writes, default root behavior, and safe secret failure handling.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-runtime-build-secret-materialization/03-CONTEXT.md
@.planning/phases/03-runtime-build-secret-materialization/03-RESEARCH.md
@.planning/phases/03-runtime-build-secret-materialization/03-VALIDATION.md
@src/unraid_actuator/config.py
@src/unraid_actuator/discovery.py
@src/unraid_actuator/build_paths.py
@src/unraid_actuator/template_render.py
@src/unraid_actuator/compose_build.py
@src/unraid_actuator/secrets.py
@src/unraid_actuator/env_materialize.py

<interfaces>
Create these build-service contracts for downstream CLI wiring:
```python
@dataclass(frozen=True)
class BuildTarget:
    app: str
    environment: str
    output_dir: Path

@dataclass(frozen=True)
class BuildResult:
    output_root: Path
    built_targets: tuple[BuildTarget, ...]

def run_build(
    *,
    runner: CommandRunner,
    config_path: Path = ACTIVE_CONFIG_PATH,
    output_root: Path | None = None,
) -> BuildResult: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Orchestrate staged runtime-tree builds and root marker writes</name>
  <files>src/unraid_actuator/build_models.py, src/unraid_actuator/build.py, tests/unit/test_build_service.py</files>
  <read_first>
    - .planning/phases/03-runtime-build-secret-materialization/03-CONTEXT.md
    - .planning/phases/03-runtime-build-secret-materialization/03-RESEARCH.md
    - src/unraid_actuator/config.py
    - src/unraid_actuator/discovery.py
    - src/unraid_actuator/build_paths.py
    - src/unraid_actuator/template_render.py
    - src/unraid_actuator/compose_build.py
    - src/unraid_actuator/secrets.py
    - src/unraid_actuator/env_materialize.py
  </read_first>
  <behavior>
    - Test 1: `run_build()` builds all discovered host environments into `/tmp/unraid-actuator/build` by default and returns the final output root plus built targets.
    - Test 2: custom non-empty output paths fail before any stage swap, and decrypt/normalize failures leave the previous final root untouched with no marker file written.
    - Test 3: successful builds write `docker-compose.yml`, merged `.env`, and `.UNRAID_RUNNING_CONFIGURATION` only after every environment succeeds.
  </behavior>
  <action>Create `src/unraid_actuator/build_models.py` with `BuildTarget` and `BuildResult`, then create `src/unraid_actuator/build.py` with `run_build(*, runner: CommandRunner, config_path: Path = ACTIVE_CONFIG_PATH, output_root: Path | None = None) -> BuildResult`. In `run_build(...)`, load the active config, resolve `host_root = config.source_path / config.hostname`, load declared apps, and discover the host tree using the shared Phase 3 source contract from 03-01. Resolve the final root with `build_paths.resolve_output_root`, call `build_paths.validate_output_root`, and create a sibling stage directory with `create_stage_root` before writing anything. Decrypt `secret-env.ejson` once per host through `decrypt_secret_env(...)`; for each discovered environment in sorted `(app, environment)` order, create `stage_root / app / environment`, normalize compose from either the static compose file or `render_template_environment(...)` plus `normalize_rendered_compose(...)`, extract environment secrets, merge them with any repo `.env` using `materialize_env_file(...)`, and write exactly `docker-compose.yml` plus `.env` into that output directory. After every environment succeeds, write the root marker file `stage_root / BUILD_MARKER_NAME` per D-10, then replace the final destination atomically with the staged tree. For the default root, overwrite any previous build tree only after the new staged tree is complete; for custom roots, rely on the earlier empty-path guard. On any failure—especially decrypt failure per BLD-06—raise a hard error, clean up the stage directory, and leave the final output root unmodified. Add `tests/unit/test_build_service.py` that uses temporary directories and `RecordingRunner` to prove default-root builds, marker-on-success behavior, and safe failure without partial final writes.</action>
  <acceptance_criteria>
    - `grep -q 'def run_build' src/unraid_actuator/build.py`
    - `grep -q '.UNRAID_RUNNING_CONFIGURATION' src/unraid_actuator/build.py`
    - `grep -q 'class BuildResult' src/unraid_actuator/build_models.py`
    - `uv run pytest tests/unit/test_build_service.py -q` passes
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/unit/test_build_service.py -q</automated>
  </verify>
  <done>The project has one reusable build service that stages a complete runtime tree, writes normalized compose/env artifacts, and swaps it into the final output path only after success.</done>
</task>

</tasks>

<verification>
Run `uv run pytest tests/unit/test_build_service.py tests/unit/test_build_paths.py -q`, then inspect a successful temporary test build tree to confirm the root marker sits at the build root rather than inside individual environment directories.
</verification>

<success_criteria>
- A full-host build now produces a staged and then swapped runtime tree.
- The default build path and custom-path safety rules are enforced by the service.
- Successful trees always include the actuator-managed root marker.
</success_criteria>

<output>
After completion, create `.planning/phases/03-runtime-build-secret-materialization/03-04-SUMMARY.md`
</output>
