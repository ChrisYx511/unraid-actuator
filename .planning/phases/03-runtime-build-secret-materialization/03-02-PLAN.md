---
phase: 03-runtime-build-secret-materialization
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/unraid_actuator/template_render.py
  - src/unraid_actuator/compose_build.py
  - tests/unit/test_template_render.py
  - tests/unit/test_compose_build.py
autonomous: true
requirements:
  - BLD-04
must_haves:
  truths:
    - "Template-driven environments render from ordered fragment includes plus values YAML only."
    - "Missing Jinja values fail the build instead of silently rendering partial compose text."
    - "Both static compose sources and rendered template sources normalize through Docker Compose without interpolation."
  artifacts:
    - path: src/unraid_actuator/template_render.py
      provides: "Ordered include loading, containment enforcement, and strict Jinja rendering"
      contains: "StrictUndefined"
    - path: src/unraid_actuator/compose_build.py
      provides: "Compose normalization helpers for static and rendered inputs"
      contains: "--no-interpolate"
    - path: tests/unit/test_template_render.py
      provides: "Executable proof of include ordering, containment, and undefined-value failures"
      contains: "test_template_render"
  key_links:
    - from: src/unraid_actuator/template_render.py
      to: src/unraid_actuator/schemas.py
      via: "Template and values files must load through the shared strict loaders"
      pattern: "load_template"
    - from: src/unraid_actuator/compose_build.py
      to: src/unraid_actuator/runner.py
      via: "Normalization goes through stdin-aware command specs with env scrubbing"
      pattern: "CommandSpec"
---

<objective>
Implement the declarative template renderer and secret-free Compose normalization pipeline that turns either allowed source shape into canonical `docker-compose.yml` text.

Purpose: Deliver the core Phase 3 transformation path while preserving deterministic, secret-free Compose output.
Output: Template rendering helpers, Compose normalization adapters, and targeted unit tests for include ordering, containment, strict undefineds, and command-spec behavior.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-runtime-build-secret-materialization/03-CONTEXT.md
@.planning/phases/03-runtime-build-secret-materialization/03-RESEARCH.md
@.planning/phases/03-runtime-build-secret-materialization/03-VALIDATION.md
@src/unraid_actuator/schemas.py
@src/unraid_actuator/discovery.py
@src/unraid_actuator/runner.py
@src/unraid_actuator/compose_validation.py

<interfaces>
Build on the Phase 3 source contract from 03-01:
```python
class SourceKind(StrEnum):
    COMPOSE = "compose"
    TEMPLATE = "template"
    MISSING = "missing"
    AMBIGUOUS = "ambiguous"

@dataclass(frozen=True)
class DiscoveredEnvironment:
    app: str
    environment: str
    path: Path
    declared: bool
    source_kind: SourceKind
    compose_files: tuple[Path, ...]
    template_file: Path | None
    values_file: Path | None
```

Reuse the stdin-aware runner pattern:
```python
@dataclass(frozen=True)
class CommandSpec:
    argv: tuple[str, ...]
    cwd: Path | None = None
    env: Mapping[str, str] = field(default_factory=dict)
    stdin_text: str | None = None
    inherit_env: bool = True
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Render declarative template environments with ordered includes and strict Jinja values</name>
  <files>src/unraid_actuator/template_render.py, tests/unit/test_template_render.py</files>
  <read_first>
    - .planning/phases/03-runtime-build-secret-materialization/03-CONTEXT.md
    - .planning/phases/03-runtime-build-secret-materialization/03-RESEARCH.md
    - src/unraid_actuator/schemas.py
    - src/unraid_actuator/discovery.py
    - tests/unit/test_schemas.py
  </read_first>
  <behavior>
    - Test 1: include files are concatenated in the exact declared order from `template.include`.
    - Test 2: any include path that escapes the environment root through `..`, absolute paths, or symlink resolution raises a hard error.
    - Test 3: undefined template values raise an error instead of rendering blank strings or reading process env/secrets.
  </behavior>
  <action>Create `src/unraid_actuator/template_render.py` for D-02 through D-06. Add a public helper named `render_template_environment(*, env_root: Path, template_path: Path, values_path: Path) -> str`. Load the descriptor and values through the `schemas.py` loaders from 03-01; do not re-parse YAML ad hoc. For every declared include, resolve the file with `Path.resolve(strict=True)` against `env_root.resolve()` and reject anything not contained within the environment root per D-03. Read UTF-8 fragment text, concatenate fragments with a single `\"\\n\"` between them in declared order per D-04, then render once with `jinja2.Environment(undefined=StrictUndefined, autoescape=False)`. Pass only the parsed `values.y[a]ml` mapping into `render(...)` per D-05; do not pass decrypted secrets, `os.environ`, or helper globals. Convert `jinja2.UndefinedError` and missing-include `FileNotFoundError` into clear `ValueError` messages that name the missing field or fragment path. Add `tests/unit/test_template_render.py` covering include ordering, containment failure, `.yaml`/`.yml` support, and strict undefined failures.</action>
  <acceptance_criteria>
    - `grep -q 'StrictUndefined' src/unraid_actuator/template_render.py`
    - `grep -q 'is_relative_to' src/unraid_actuator/template_render.py`
    - `uv run pytest tests/unit/test_template_render.py -q` passes
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/unit/test_template_render.py -q</automated>
  </verify>
  <done>Template-driven environments render from ordered fragment includes and values-only Jinja input, and any missing value or escaping path fails immediately.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Normalize static and rendered Compose sources without interpolation</name>
  <files>src/unraid_actuator/compose_build.py, tests/unit/test_compose_build.py</files>
  <read_first>
    - .planning/phases/03-runtime-build-secret-materialization/03-CONTEXT.md
    - .planning/phases/03-runtime-build-secret-materialization/03-RESEARCH.md
    - src/unraid_actuator/runner.py
    - src/unraid_actuator/compose_validation.py
    - tests/unit/test_compose_validation.py
  </read_first>
  <behavior>
    - Test 1: static compose normalization invokes Docker Compose with `--no-interpolate --format yaml` and `COMPOSE_DISABLE_ENV_FILE=1`.
    - Test 2: rendered template text is normalized through stdin using `-f -` with the same env scrubbing.
    - Test 3: non-zero Compose exit codes raise a hard error that includes stderr/stdout rather than returning partial output.
  </behavior>
  <action>Create `src/unraid_actuator/compose_build.py` with two helpers: `normalize_static_compose(*, source_env_dir: Path, compose_file: Path, project_name: str, runner: CommandRunner) -> str` and `normalize_rendered_compose(*, source_env_dir: Path, rendered_text: str, project_name: str, runner: CommandRunner) -> str`. For static compose, run `docker compose -p {project_name} --project-directory {source_env_dir} -f {compose_file.name} config --no-interpolate --format yaml`. For rendered text, run `docker compose -p {project_name} --project-directory {source_env_dir} -f - config --no-interpolate --format yaml` with `stdin_text=rendered_text`. In both cases set `env={\"COMPOSE_DISABLE_ENV_FILE\": \"1\"}` and `inherit_env=False` so normalized YAML does not absorb ambient shell values or local `.env` files. Return `result.stdout` on success and raise `ValueError` with the command output on failure. Add `tests/unit/test_compose_build.py` using `RecordingRunner` to prove the exact command specs for static and rendered sources and the failure behavior.</action>
  <acceptance_criteria>
    - `grep -q -- '--no-interpolate' src/unraid_actuator/compose_build.py`
    - `grep -q 'COMPOSE_DISABLE_ENV_FILE' src/unraid_actuator/compose_build.py`
    - `uv run pytest tests/unit/test_compose_build.py -q` passes
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/unit/test_compose_build.py -q</automated>
  </verify>
  <done>Both allowed source shapes can now be normalized into canonical Compose YAML without interpolation, secret leakage, or ambient environment drift.</done>
</task>

</tasks>

<verification>
Run `uv run pytest tests/unit/test_template_render.py tests/unit/test_compose_build.py -q`, then inspect `src/unraid_actuator/compose_build.py` to confirm both helper commands include `--no-interpolate`, `--format yaml`, and `COMPOSE_DISABLE_ENV_FILE=1`.
</verification>

<success_criteria>
- Template rendering uses include ordering, strict containment, and values-only Jinja input.
- Compose normalization is deterministic for both static and rendered sources.
- Failure cases surface as hard build errors instead of partial normalized output.
</success_criteria>

<output>
After completion, create `.planning/phases/03-runtime-build-secret-materialization/03-02-SUMMARY.md`
</output>
