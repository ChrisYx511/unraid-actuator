---
phase: 03-runtime-build-secret-materialization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/unraid_actuator/validation_models.py
  - src/unraid_actuator/schemas.py
  - src/unraid_actuator/discovery.py
  - src/unraid_actuator/build_paths.py
  - tests/unit/test_discovery.py
  - tests/unit/test_schemas.py
  - tests/unit/test_build_paths.py
autonomous: true
requirements:
  - BLD-02
  - BLD-03
  - BLD-04
must_haves:
  truths:
    - "Template-driven environments are recognized through declarative `template.y[a]ml` plus mapping-only `values.y[a]ml`, not repo-executed `build.py`."
    - "The build command has one deterministic default output root and rejects unsafe custom output roots before any build work starts."
    - "Operators get the same `apps.y[a]ml` host declaration resolution and compose-versus-template source decision anywhere the actuator inspects a host."
  artifacts:
    - path: src/unraid_actuator/schemas.py
      provides: "Strict loaders for host-root `apps.y[a]ml`, template descriptors, and mapping-only values YAML"
      contains: "template"
    - path: src/unraid_actuator/discovery.py
      provides: "Shared compose-vs-template source classification"
      contains: "SourceKind.TEMPLATE"
    - path: src/unraid_actuator/build_paths.py
      provides: "Output-root validation, stage-root helpers, and build marker constants"
      contains: "DEFAULT_BUILD_ROOT"
  key_links:
    - from: src/unraid_actuator/discovery.py
      to: src/unraid_actuator/schemas.py
      via: "Host declaration resolution and template source discovery must both call the same shared `.yml` / `.yaml` loaders used later by build and validation"
      pattern: "resolve_host_apps_path"
    - from: src/unraid_actuator/build_paths.py
      to: src/unraid_actuator/build.py
      via: "Build orchestration must call the shared output-root guard before staging any files"
      pattern: "validate_output_root"
---

<objective>
Replace the stale `build.py` source assumptions with shared template/value discovery contracts, and add the output-root safety primitives every later build plan will reuse.

Purpose: Lock the Phase 3 foundation first so rendering, secret handling, build orchestration, and validation all use the same declarative source and path rules.
Output: Updated source-kind/shared schema logic, build-path helpers, dependency declarations, and unit coverage for template detection plus custom output safety.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-runtime-build-secret-materialization/03-CONTEXT.md
@.planning/phases/03-runtime-build-secret-materialization/03-RESEARCH.md
@.planning/phases/03-runtime-build-secret-materialization/03-VALIDATION.md
@src/unraid_actuator/discovery.py
@src/unraid_actuator/schemas.py
@src/unraid_actuator/validation_models.py

<interfaces>
Existing contracts to update in place:
```python
class SourceKind(StrEnum):
    COMPOSE = "compose"
    BUILD_PY = "build_py"
    MISSING = "missing"
    AMBIGUOUS = "ambiguous"

@dataclass(frozen=True)
class DiscoveredEnvironment:
    app: str
    environment: str
    path: Path
    declared: bool
    source_kind: SourceKind
    compose_files: tuple[Path, ...]
    build_file: Path | None
```

Phase 3 must replace the repo-side `build.py` branch with declarative template inputs:
```python
class SourceKind(StrEnum):
    COMPOSE = "compose"
    TEMPLATE = "template"
    MISSING = "missing"
    AMBIGUOUS = "ambiguous"

@dataclass(frozen=True)
class DiscoveredEnvironment:
    app: str
    environment: str
    path: Path
    declared: bool
    source_kind: SourceKind
    compose_files: tuple[Path, ...]
    template_file: Path | None
    values_file: Path | None
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace `build.py` discovery with declarative template/value source contracts</name>
  <files>src/unraid_actuator/validation_models.py, src/unraid_actuator/schemas.py, src/unraid_actuator/discovery.py, tests/unit/test_discovery.py, tests/unit/test_schemas.py</files>
  <read_first>
    - .planning/phases/03-runtime-build-secret-materialization/03-CONTEXT.md
    - .planning/phases/03-runtime-build-secret-materialization/03-RESEARCH.md
    - src/unraid_actuator/validation_models.py
    - src/unraid_actuator/schemas.py
    - src/unraid_actuator/discovery.py
    - tests/unit/test_discovery.py
    - tests/unit/test_schemas.py
  </read_first>
  <behavior>
    - Test 1: discovery marks one `docker-compose.y[a]ml` file as `SourceKind.COMPOSE`, one `template.y[a]ml` plus one `values.y[a]ml` pair as `SourceKind.TEMPLATE`, and no recognized files as `SourceKind.MISSING`.
    - Test 2: compose-plus-template mixes, duplicate `.yml`/`.yaml` variants, or template-without-values cases return `SourceKind.AMBIGUOUS` so later validation can fail deterministically.
    - Test 3: strict schema helpers resolve exactly one host-root `apps.yaml` or `apps.yml`, load `template.yml` / `template.yaml` as `template.include: [relative/path]`, and load `values.yml` / `values.yaml` as a mapping/object root through `strictyaml`, preserving nested values under top-level keys for Jinja without permitting repo-executed Python.
  </behavior>
  <action>Update the shared source contract per D-01, D-02, D-03, and D-11. In `src/unraid_actuator/validation_models.py`, replace `SourceKind.BUILD_PY` with `SourceKind.TEMPLATE`, remove the `build_file` field, and add `template_file: Path | None` plus `values_file: Path | None` to `DiscoveredEnvironment`. In `src/unraid_actuator/schemas.py`, keep the existing secret helpers but replace any canonical-only host declaration assumption with one shared resolver/helper, `resolve_host_apps_path(host_root: Path) -> Path`, that accepts exactly one of `apps.yaml` or `apps.yml`, raises a deterministic error if both are present, and is the contract later plans must reuse anywhere they load host declarations per D-11. Add strict loaders for both `template.yml` and `template.yaml` using a schema equivalent to `Map({\"template\": Map({\"include\": Seq(Str())})})`, and add a `values.y[a]ml` loader whose root is mapping/object only (for example `MapPattern(Str(), Any())`) so nested maps/lists/scalars remain available beneath top-level keys for Jinja while non-mapping roots fail fast. In `src/unraid_actuator/discovery.py`, classify valid sources as exactly one compose file or exactly one template descriptor plus exactly one values file, with every file resolved from the environment directory and both `.yaml` / `.yml` variants accepted per D-11. Treat compose+template mixes, duplicate extension variants, template-without-values, or values-without-template as `SourceKind.AMBIGUOUS`; treat the absence of all source files as `SourceKind.MISSING`. Update `tests/unit/test_discovery.py` and `tests/unit/test_schemas.py` to prove the shared `apps.y[a]ml` resolver, the new template/value contract, mapping-only values loading, `.yaml`/`.yml` support, and ambiguous-partial-source cases. Do not leave any repo-side `build.py` discovery path in this shared contract.</action>
  <acceptance_criteria>
    - `grep -q 'TEMPLATE = "template"' src/unraid_actuator/validation_models.py`
    - `grep -q 'resolve_host_apps_path' src/unraid_actuator/schemas.py`
    - `grep -q 'template' src/unraid_actuator/schemas.py`
    - `grep -q 'values_file' src/unraid_actuator/discovery.py`
    - `uv run pytest tests/unit/test_discovery.py tests/unit/test_schemas.py -q` passes
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/unit/test_discovery.py tests/unit/test_schemas.py -q</automated>
  </verify>
  <done>The repo has one typed, strict host-declaration and compose-versus-template discovery contract that accepts `.yaml` and `.yml` variants, enforces mapping-only values inputs, and fully removes repo-executed `build.py` assumptions from shared Phase 3 inputs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add build-path safety helpers and declare the rendering/materialization dependencies</name>
  <files>pyproject.toml, src/unraid_actuator/build_paths.py, tests/unit/test_build_paths.py</files>
  <read_first>
    - .planning/phases/03-runtime-build-secret-materialization/03-CONTEXT.md
    - .planning/phases/03-runtime-build-secret-materialization/03-RESEARCH.md
    - pyproject.toml
    - src/unraid_actuator/config.py
  </read_first>
  <behavior>
    - Test 1: the default build root resolves to `/tmp/unraid-actuator/build`.
    - Test 2: a custom output path is allowed only when it does not exist yet or exists as an empty directory.
    - Test 3: a non-default non-empty directory fails before any stage directory or runtime files are created.
  </behavior>
  <action>Add the new project dependencies from research to `pyproject.toml`: keep `strictyaml>=1.7.3,<2` and append `Jinja2>=3.1.6,<4` plus `python-dotenv>=1.2.2,<2` to `[project.dependencies]`. Then create `src/unraid_actuator/build_paths.py` with concrete helpers for BLD-02 and BLD-03: `DEFAULT_BUILD_ROOT = Path(\"/tmp/unraid-actuator/build\")`, `BUILD_MARKER_NAME = \".UNRAID_RUNNING_CONFIGURATION\"`, `resolve_output_root(output_root: Path | None) -> Path`, `validate_output_root(final_root: Path) -> None`, and `create_stage_root(final_root: Path) -> Path`. `validate_output_root(...)` must allow the default path even when it already exists, but for any custom path it must raise `ValueError(f\"custom build output path must be empty before build: {final_root}\")` when the directory exists and contains anything, or when the path exists as a non-directory. `create_stage_root(...)` should create a sibling temp directory under `final_root.parent` so later plans can stage-then-swap safely. Add `tests/unit/test_build_paths.py` covering the default root constant, empty/missing custom-path acceptance, and non-empty custom-path safe failure. Do not write runtime artifacts in this plan; only provide the shared path guards and constants later plans will call.</action>
  <acceptance_criteria>
    - `grep -q 'Jinja2>=3.1.6,<4' pyproject.toml`
    - `grep -q 'python-dotenv>=1.2.2,<2' pyproject.toml`
    - `grep -q 'DEFAULT_BUILD_ROOT = Path(\"/tmp/unraid-actuator/build\")' src/unraid_actuator/build_paths.py`
    - `uv run pytest tests/unit/test_build_paths.py -q` passes
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/unit/test_build_paths.py -q</automated>
  </verify>
  <done>The codebase has explicit dependency declarations plus reusable output-root helpers that reject unsafe custom destinations before any build work starts.</done>
</task>

</tasks>

<verification>
Run `uv run pytest tests/unit/test_discovery.py tests/unit/test_schemas.py tests/unit/test_build_paths.py -q` and confirm there are no remaining shared-contract references to repo-side `build.py` in `src/unraid_actuator/discovery.py`.
</verification>

<success_criteria>
- Shared source classification now recognizes compose or declarative template/value inputs only.
- Both `.yaml` and `.yml` variants are accepted for Phase 3 YAML-backed inputs.
- Default/custom build-root behavior is locked behind one reusable safety helper module.
</success_criteria>

<output>
After completion, create `.planning/phases/03-runtime-build-secret-materialization/03-01-SUMMARY.md`
</output>
