---
phase: 04-safe-deploy-teardown
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - src/unraid_actuator/cli.py
  - tests/unit/test_deploy_cli.py
autonomous: true
requirements:
  - DEP-02
  - DEP-03
  - DEP-04
must_haves:
  truths:
    - "Operator can call `deploy` or `teardown` against the default build root or an explicit `--build-root`."
    - "Providing only one of `--app` or `--environment` fails immediately instead of guessing intent."
    - "CLI returns exit code 1 for runtime-tree/service/schema failures and remains dry-run/test friendly."
  artifacts:
    - path: src/unraid_actuator/cli.py
      provides: "Thin public `deploy` and `teardown` subcommands"
      contains: "\"deploy\""
    - path: tests/unit/test_deploy_cli.py
      provides: "Proof of paired-scope handling, service dispatch, and error exits for both commands"
      contains: "test_deploy_requires_both_scope_flags"
  key_links:
    - from: src/unraid_actuator/cli.py
      to: src/unraid_actuator/deploy.py
      via: "CLI must route parsed args into `run_deploy(...)` / `run_teardown(...)` with injected runner and config path"
      pattern: "run_deploy"
---

<objective>
Expose deploy and teardown safely at the public CLI boundary without breaking the thin argparse-to-service pattern established earlier.

Purpose: Finish Phase 4 as an operator-facing feature with paired scope flags, default/custom build-root selection, and clear safe-failure behavior.
Output: New CLI subcommands, exit-code handling, and unit tests for deploy/teardown parser behavior and dispatch.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-safe-deploy-teardown/04-CONTEXT.md
@.planning/phases/04-safe-deploy-teardown/04-RESEARCH.md
@.planning/phases/04-safe-deploy-teardown/04-VALIDATION.md
@src/unraid_actuator/cli.py
@src/unraid_actuator/build_paths.py
@src/unraid_actuator/deploy.py
@src/unraid_actuator/config.py

<interfaces>
Use the service boundary from 04-02:
```python
def run_deploy(
    *,
    runner: CommandRunner,
    config_path: Path = ACTIVE_CONFIG_PATH,
    build_root: Path | None = None,
    app: str | None = None,
    environment: str | None = None,
) -> RuntimeActionResult: ...

def run_teardown(
    *,
    runner: CommandRunner,
    config_path: Path = ACTIVE_CONFIG_PATH,
    build_root: Path | None = None,
    app: str | None = None,
    environment: str | None = None,
) -> RuntimeActionResult: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the public `deploy` subcommand with paired scope validation</name>
  <files>src/unraid_actuator/cli.py, tests/unit/test_deploy_cli.py</files>
  <read_first>
    - .planning/phases/04-safe-deploy-teardown/04-CONTEXT.md
    - src/unraid_actuator/cli.py
    - src/unraid_actuator/deploy.py
    - tests/unit/test_validate_cli.py
    - tests/unit/test_build_cli.py
  </read_first>
  <behavior>
    - Test 1: `unraid-actuator deploy --app foo` or `--environment prod` alone exits through `argparse` with the same paired-scope error style used by `validate`, per DEP-04.
    - Test 2: `unraid-actuator deploy` dispatches an unscoped service call against the default build root, while `--build-root /custom --app foo --environment bar` passes exact Path/selectors through to `run_deploy(...)`.
    - Test 3: service or schema errors print the original message to stderr and return exit code `1`.
  </behavior>
  <action>Update `src/unraid_actuator/cli.py` to add a `deploy` subcommand that keeps the existing thin parser/service boundary. Add optional `--build-root`, `--app`, and `--environment` flags. Reuse the paired-scope parser rule from `validate`: if exactly one selector is provided, call `parser.error("--app and --environment must be provided together")` per DEP-04 and D-02. Dispatch to `run_deploy(...)` with `build_root=Path(args.build_root) if args.build_root else None`, the injected `runner`, and `config_path`. On success, print a concise summary that names the action, target count, and build root; exact wording is discretionary. Catch `YAMLValidationError` and `ValueError`, print the original message to stderr, and return `1`. Add deploy-focused cases to `tests/unit/test_deploy_cli.py` first so the command surface, paired scope handling, default/custom build-root dispatch, and failure exit behavior are locked before implementation.</action>
  <acceptance_criteria>
    - `grep -q 'add_parser("deploy"' src/unraid_actuator/cli.py`
    - `grep -q -- '--build-root' src/unraid_actuator/cli.py`
    - `uv run pytest tests/unit/test_deploy_cli.py -q` passes
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/unit/test_deploy_cli.py -q</automated>
  </verify>
  <done>The CLI exposes `deploy`, rejects incomplete scope safely, and routes full-tree or scoped execution into the service layer without adding deployment logic to the parser.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the public `teardown` subcommand and final CLI behavior coverage</name>
  <files>src/unraid_actuator/cli.py, tests/unit/test_deploy_cli.py</files>
  <read_first>
    - src/unraid_actuator/cli.py
    - src/unraid_actuator/deploy.py
    - tests/unit/test_deploy_cli.py
  </read_first>
  <behavior>
    - Test 1: `teardown` mirrors the exact paired-scope rule and `--build-root` dispatch contract from deploy.
    - Test 2: `teardown` routes to `run_teardown(...)` for either full-tree or one exact app/environment pair, with the same dry-run/injected-runner surface as other commands.
    - Test 3: the final CLI test file proves both commands coexist with existing `init`, `validate`, and `build` behavior rather than replacing them.
  </behavior>
  <action>Extend the same CLI surface with a `teardown` subcommand using the identical `--build-root`, `--app`, and `--environment` semantics as `deploy`. Route it to `run_teardown(...)` with the injected runner/config path, and keep safe error handling identical to deploy so schema and runtime-tree failures remain explicit. Reuse the same paired selector validation per DEP-04, and do not guess scope or silently promote a scoped request into a full-tree action. Expand `tests/unit/test_deploy_cli.py` to cover teardown dispatch, teardown-specific paired-scope failures, and coexistence with the existing command set. Keep the CLI thin: no target discovery, build-root trust, or Compose logic should live here.</action>
  <acceptance_criteria>
    - `grep -q 'add_parser("teardown"' src/unraid_actuator/cli.py`
    - `grep -q 'run_teardown' src/unraid_actuator/cli.py`
    - `uv run pytest tests/unit/test_deploy_cli.py -q` passes
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/unit/test_deploy_cli.py -q</automated>
  </verify>
  <done>The public CLI now exposes both `deploy` and `teardown` with safe paired-scope handling, custom/default build-root support, and service-only orchestration.</done>
</task>

</tasks>

<verification>
Run `uv run pytest tests/unit/test_deploy_cli.py -q`, then run `uv run python -m unraid_actuator deploy --help` and `uv run python -m unraid_actuator teardown --help` to confirm the public surface exposes `--build-root`, `--app`, and `--environment` without changing the earlier commands.

Manual host check deferred because Docker is unavailable here: on a Docker-equipped host, run `unraid-actuator deploy --build-root <marked-root>` and `unraid-actuator teardown --build-root <marked-root>` against a known runtime tree to confirm the operator-facing command surface works end to end.
</verification>

<success_criteria>
- `deploy` and `teardown` are publicly available commands.
- Partial scope flags fail before any service work begins.
- CLI wiring remains thin, injected-runner friendly, and aligned with earlier command patterns.
</success_criteria>

<output>
After completion, create `.planning/phases/04-safe-deploy-teardown/04-03-SUMMARY.md`
</output>
