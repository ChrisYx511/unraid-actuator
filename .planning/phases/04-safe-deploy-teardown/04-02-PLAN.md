---
phase: 04-safe-deploy-teardown
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/unraid_actuator/deploy.py
  - tests/unit/test_deploy_service.py
  - tests/unit/test_teardown_service.py
autonomous: true
requirements:
  - DEP-01
  - DEP-02
  - DEP-03
must_haves:
  truths:
    - "Full-tree deploy stops on the first failed target instead of continuing."
    - "Full-tree teardown uses the same fail-fast ordered batch behavior over the marked runtime tree."
    - "Scoped deploy and teardown operate on exactly one validated target and never widen scope implicitly."
  artifacts:
    - path: src/unraid_actuator/deploy.py
      provides: "Deploy and teardown orchestration services"
      contains: "run_deploy"
    - path: tests/unit/test_deploy_service.py
      provides: "Proof of ordered fail-fast deploy behavior and scoped revalidation"
      contains: "test_deploy_full_is_ordered_and_fail_fast"
    - path: tests/unit/test_teardown_service.py
      provides: "Proof of ordered fail-fast teardown behavior and scoped revalidation"
      contains: "test_teardown_full_is_ordered_and_fail_fast"
  key_links:
    - from: src/unraid_actuator/deploy.py
      to: src/unraid_actuator/deploy_tree.py
      via: "Services must resolve full-tree and scoped targets through the shared trust/selection helpers"
      pattern: "resolve_full_tree_targets"
    - from: src/unraid_actuator/deploy.py
      to: src/unraid_actuator/compose_runtime.py
      via: "Services must execute only the explicit Compose command specs created in 04-01"
      pattern: "compose_up_spec"
---

<objective>
Implement the actual deploy/teardown orchestration on top of the trusted runtime-tree and command-spec contracts from 04-01.

Purpose: Deliver the Phase 4 service layer with ordered, fail-fast execution and a hard distinction between full-tree trust and scoped current-host validity.
Output: `run_deploy(...)`, `run_teardown(...)`, structured results, and unit coverage for deploy/teardown behavior without live Docker.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-safe-deploy-teardown/04-CONTEXT.md
@.planning/phases/04-safe-deploy-teardown/04-RESEARCH.md
@.planning/phases/04-safe-deploy-teardown/04-VALIDATION.md
@src/unraid_actuator/build_paths.py
@src/unraid_actuator/runner.py
@src/unraid_actuator/deploy_models.py
@src/unraid_actuator/deploy_tree.py
@src/unraid_actuator/compose_runtime.py

<interfaces>
Use the 04-01 contracts directly:
```python
@dataclass(frozen=True)
class RuntimeTarget:
    app: str
    environment: str
    output_dir: Path
    compose_file: Path
    env_file: Path

@dataclass(frozen=True)
class RuntimeActionResult:
    build_root: Path
    targets: tuple[RuntimeTarget, ...]
    command_results: tuple[CommandResult, ...]

def resolve_full_tree_targets(*, build_root: Path, config_path: Path = ACTIVE_CONFIG_PATH) -> tuple[RuntimeTarget, ...]: ...
def resolve_scoped_target(*, build_root: Path, config_path: Path = ACTIVE_CONFIG_PATH, app: str, environment: str) -> RuntimeTarget: ...
def compose_up_spec(target: RuntimeTarget) -> CommandSpec: ...
def compose_down_spec(target: RuntimeTarget) -> CommandSpec: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement full-tree and scoped deploy orchestration</name>
  <files>src/unraid_actuator/deploy.py, tests/unit/test_deploy_service.py</files>
  <read_first>
    - .planning/phases/04-safe-deploy-teardown/04-CONTEXT.md
    - .planning/phases/04-safe-deploy-teardown/04-RESEARCH.md
    - src/unraid_actuator/runner.py
    - src/unraid_actuator/deploy_models.py
    - src/unraid_actuator/deploy_tree.py
    - src/unraid_actuator/compose_runtime.py
    - tests/unit/test_build_service.py
  </read_first>
  <behavior>
    - Test 1: `run_deploy()` with no scope resolves the default build root, trusts only a marked runtime tree per DEP-01, executes targets in declaration-first order per D-03/D-05, and stops on the first non-zero compose result per D-01.
    - Test 2: `run_deploy(app=..., environment=...)` requires both selectors together, revalidates the scoped target against current host membership per D-02/D-05, and executes exactly one `up -d` spec.
    - Test 3: dry-run and recording runners still return structured results without needing live Docker, and deploy errors surface the target name plus stderr/stdout context.
  </behavior>
  <action>Update `src/unraid_actuator/deploy.py` to add `run_deploy(*, runner: CommandRunner, config_path: Path = ACTIVE_CONFIG_PATH, build_root: Path | None = None, app: str | None = None, environment: str | None = None) -> RuntimeActionResult`. Guard misuse inside the service too: if exactly one selector is provided, raise `ValueError("--app and --environment must be provided together")` so DEP-04 is safe even outside the CLI. Use `resolve_output_root(build_root)` for the default runtime root. For unscoped deploy, call `resolve_full_tree_targets(...)` and loop in returned order, building specs only through `compose_up_spec(...)`; stop immediately on the first failed result per D-01 and raise a target-specific error that includes the failing app/environment and compose stderr/stdout. For scoped deploy, call `resolve_scoped_target(...)` so current-host validity is enforced per D-02/D-05 before any command runs. Return `RuntimeActionResult` with the resolved build root, targets executed, and collected `CommandResult`s. Do not add retries, reconciliation logic, or `--remove-orphans`; Phase 4 is direct safe apply only per D-04.</action>
  <acceptance_criteria>
    - `grep -q "def run_deploy" src/unraid_actuator/deploy.py`
    - `grep -q "compose_up_spec" src/unraid_actuator/deploy.py`
    - `uv run pytest tests/unit/test_deploy_service.py -q` passes
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/unit/test_deploy_service.py -q</automated>
  </verify>
  <done>Deploy orchestration is runner-based, fail-fast, declaration-ordered, and safely distinguishes full-tree trust from scoped current-host revalidation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement full-tree and scoped teardown orchestration</name>
  <files>src/unraid_actuator/deploy.py, tests/unit/test_teardown_service.py</files>
  <read_first>
    - .planning/phases/04-safe-deploy-teardown/04-CONTEXT.md
    - .planning/phases/04-safe-deploy-teardown/04-RESEARCH.md
    - src/unraid_actuator/deploy.py
    - src/unraid_actuator/deploy_tree.py
    - src/unraid_actuator/compose_runtime.py
  </read_first>
  <behavior>
    - Test 1: full-tree teardown runs over the same ordered target list as deploy and stops on the first failure per D-01/D-03/D-05.
    - Test 2: scoped teardown requires exact selector pairing and current-host validity per D-02/D-05, even if the marked build tree still contains stale directories allowed for full-tree teardown.
    - Test 3: teardown uses only plain `docker compose down` command specs and preserves dry-run/recording behavior through the same result contract.
  </behavior>
  <action>Extend `src/unraid_actuator/deploy.py` with `run_teardown(*, runner: CommandRunner, config_path: Path = ACTIVE_CONFIG_PATH, build_root: Path | None = None, app: str | None = None, environment: str | None = None) -> RuntimeActionResult`. Mirror the selector guard from deploy. Reuse `resolve_output_root(...)`, `resolve_full_tree_targets(...)`, and `resolve_scoped_target(...)` so full-tree trust remains marker-based while scoped actions stay current-host-valid per D-02/D-05. Execute only `compose_down_spec(...)`; do not add `-v`, `--rmi`, or orphan-removal flags. Fail fast on the first non-zero result per D-01 and report the failing target explicitly. Add `tests/unit/test_teardown_service.py` to prove declaration-order execution, scoped revalidation, fail-fast stop, and exact command-spec usage for teardown.</action>
  <acceptance_criteria>
    - `grep -q "def run_teardown" src/unraid_actuator/deploy.py`
    - `grep -q "compose_down_spec" src/unraid_actuator/deploy.py`
    - `uv run pytest tests/unit/test_teardown_service.py -q` passes
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/unit/test_teardown_service.py -q</automated>
  </verify>
  <done>Teardown orchestration matches the deploy safety model: same ordered target selection, same scoped validity gate, and same first-failure stop behavior.</done>
</task>

</tasks>

<verification>
Run `uv run pytest tests/unit/test_deploy_service.py tests/unit/test_teardown_service.py -q`.

Manual host check deferred because Docker is unavailable here: on a Docker-equipped host, run one full-tree deploy and one full-tree teardown against a marked runtime tree, then repeat with a deliberately broken second target to confirm the batch stops immediately after the first failure.
</verification>

<success_criteria>
- `run_deploy(...)` and `run_teardown(...)` exist and use the shared 04-01 contracts.
- Full-tree actions are ordered and fail-fast.
- Scoped actions execute exactly one build-tree target only after current-host revalidation.
</success_criteria>

<output>
After completion, create `.planning/phases/04-safe-deploy-teardown/04-02-SUMMARY.md`
</output>
