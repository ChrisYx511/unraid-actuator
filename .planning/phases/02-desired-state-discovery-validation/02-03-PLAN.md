---
phase: 02-desired-state-discovery-validation
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/unraid_actuator/runner.py
  - src/unraid_actuator/compose_validation.py
  - src/unraid_actuator/report.py
  - tests/unit/test_runner.py
  - tests/unit/test_compose_validation.py
  - tests/unit/test_report_renderer.py
autonomous: true
requirements:
  - VAL-06
must_haves:
  truths:
    - "Operator receives a validation failure when a static Compose environment does not pass Compose config checks."
    - "Operator receives a validation failure when a `build.py` environment emits invalid Compose output, without any secret decryption during validation."
    - "Operator sees one grouped human-readable report with separate Errors and Warnings sections plus final summary counts."
  artifacts:
    - path: src/unraid_actuator/compose_validation.py
      provides: "Static and dynamic Compose preflight helpers using the shared runner"
      contains: "docker"
    - path: src/unraid_actuator/report.py
      provides: "Grouped human-readable validation report formatting"
      contains: "Summary:"
    - path: src/unraid_actuator/runner.py
      provides: "CommandSpec support for stdin piping and scrubbed environments"
      contains: "stdin_text"
  key_links:
    - from: src/unraid_actuator/compose_validation.py
      to: src/unraid_actuator/runner.py
      via: "Compose validation commands executed through `CommandSpec` and `CommandRunner`"
      pattern: "docker"
    - from: src/unraid_actuator/report.py
      to: src/unraid_actuator/validation_models.py
      via: "Renderer groups findings by `FindingSeverity` and prints counts from `ValidationReport`"
      pattern: "warning_count"
---

<objective>
Add the subprocess adapter and renderer needed for secret-free Compose preflight checks and operator-facing validation output.

Purpose: Keep external command execution and report formatting isolated so the final service can orchestrate them without mixing subprocess details into CLI parsing.
Output: Runner support for stdin/env control, Compose validation helpers for static and dynamic sources, and grouped report rendering/tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-desired-state-discovery-validation/02-CONTEXT.md
@.planning/phases/02-desired-state-discovery-validation/02-RESEARCH.md
@src/unraid_actuator/runner.py
@src/unraid_actuator/validation_models.py

<interfaces>
Existing runner contract from `src/unraid_actuator/runner.py`:
```python
@dataclass(frozen=True)
class CommandSpec:
    argv: tuple[str, ...]
    cwd: Path | None = None
    env: Mapping[str, str] = field(default_factory=dict)
```

Extend it in this plan to:
```python
@dataclass(frozen=True)
class CommandSpec:
    argv: tuple[str, ...]
    cwd: Path | None = None
    env: Mapping[str, str] = field(default_factory=dict)
    stdin_text: str | None = None
    inherit_env: bool = True
```

Create these adapter helpers in `src/unraid_actuator/compose_validation.py`:
```python
def validate_static_compose(candidate: DiscoveredEnvironment, project_name: str, *, runner: CommandRunner) -> tuple[ValidationFinding, ...]: ...
def validate_dynamic_build(candidate: DiscoveredEnvironment, project_name: str, *, runner: CommandRunner) -> tuple[ValidationFinding, ...]: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the runner contract for stdin piping and implement Compose preflight helpers</name>
  <files>src/unraid_actuator/runner.py, src/unraid_actuator/compose_validation.py, tests/unit/test_runner.py, tests/unit/test_compose_validation.py</files>
  <read_first>
    - .planning/phases/02-desired-state-discovery-validation/02-CONTEXT.md (D-01, D-02)
    - .planning/phases/02-desired-state-discovery-validation/02-RESEARCH.md (Pattern 3, Recommended Validation Flow steps 8-9, Pitfall 3, Pitfall 4)
    - src/unraid_actuator/runner.py
    - src/unraid_actuator/validation_models.py
  </read_first>
  <behavior>
    - Test 1: `CommandSpec` can carry `stdin_text` and opt out of inheriting ambient environment variables without breaking existing callers.
    - Test 2: static Compose validation produces a `docker compose -p {project_name} -f {compose_filename} config -q` command spec in the candidate environment directory.
    - Test 3: dynamic validation runs `sys.executable -I build.py`, captures stdout, and passes that stdout into `docker compose -p {project_name} -f - config -q` through `stdin_text`, with no secret decryption and no ambient env inheritance.
  </behavior>
  <action>Update `src/unraid_actuator/runner.py` so `CommandSpec` gains `stdin_text: str | None = None` and `inherit_env: bool = True`. Modify `SubprocessRunner.run()` to pass `input=spec.stdin_text` into `subprocess.run(...)` and to use `env={**os.environ, **spec.env}` only when `inherit_env` is true; otherwise pass `dict(spec.env)` exactly. Keep `DryRunRunner` and `RecordingRunner` backward compatible while preserving the new fields in captured specs/results. Then create `src/unraid_actuator/compose_validation.py` with two helpers: `validate_static_compose(...)` that shells out to `docker compose -p {project_name} -f {compose_filename.name} config -q`, and `validate_dynamic_build(...)` that runs `(sys.executable, "-I", "build.py")` in the environment directory, requires non-empty stdout, and pipes that stdout into `docker compose -p {project_name} -f - config -q` using `stdin_text`. Both helpers must return `ValidationFinding` tuples instead of raising on command failures, use `inherit_env=False` and `env={}` for Compose calls, and must not decrypt secrets or inspect secret values per D-01/D-02.</action>
  <acceptance_criteria>
    - `grep -q "stdin_text" src/unraid_actuator/runner.py`
    - `grep -q "docker\", \"compose\"" src/unraid_actuator/compose_validation.py`
    - `uv run pytest tests/unit/test_runner.py tests/unit/test_compose_validation.py -q` passes
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/unit/test_runner.py tests/unit/test_compose_validation.py -q</automated>
  </verify>
  <done>The shared runner can now model stdin and scrubbed environments, and Phase 2 has reusable helpers for validating both static Compose files and `build.py` output through the official Docker Compose validator.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render grouped human-readable validation reports</name>
  <files>src/unraid_actuator/report.py, tests/unit/test_report_renderer.py</files>
  <read_first>
    - .planning/phases/02-desired-state-discovery-validation/02-CONTEXT.md (D-07)
    - .planning/phases/02-desired-state-discovery-validation/02-RESEARCH.md (Primary recommendation, D-07 summary)
    - src/unraid_actuator/validation_models.py
  </read_first>
  <behavior>
    - Test 1: the renderer prints an `Errors` section and a `Warnings` section separately, even when one group is empty.
    - Test 2: findings are rendered with app/environment scope when present so one run exposes the whole broken desired state.
    - Test 3: the output ends with a final summary count line containing at least checked-target, error, and warning totals.
  </behavior>
  <action>Create `src/unraid_actuator/report.py` with a single public function `render_validation_report(report: ValidationReport, *, hostname: str) -> str`. Format the output as a grouped human-readable report per D-07: heading, `Errors (N)`, `Warnings (N)`, then `Summary: checked={count} errors={count} warnings={count}`. Render each finding on its own line using a stable shape such as `- [app/environment] CODE: message` when scope exists, otherwise `- CODE: message`. Do not print directly in this module; return one string so the CLI can own stdout/stderr behavior. Add `tests/unit/test_report_renderer.py` that locks this grouping/count format.</action>
  <acceptance_criteria>
    - `grep -q "render_validation_report" src/unraid_actuator/report.py`
    - `grep -q "Summary:" src/unraid_actuator/report.py`
    - `uv run pytest tests/unit/test_report_renderer.py -q` passes
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/unit/test_report_renderer.py -q</automated>
  </verify>
  <done>Validation now has one reusable renderer that produces grouped operator-facing output with separate errors/warnings and explicit summary counts.</done>
</task>

</tasks>

<verification>
Run `uv run pytest tests/unit/test_runner.py tests/unit/test_compose_validation.py tests/unit/test_report_renderer.py -q` to confirm the subprocess adapter and renderer are stable before final orchestration wiring.
</verification>

<success_criteria>
- The runner supports stdin-fed commands and scrubbed environments without breaking existing dry-run/recording behavior.
- Static Compose and dynamic `build.py` outputs are both representable as official `docker compose config -q` checks.
- One renderer produces the grouped report format required by Phase 2.
</success_criteria>

<output>
After completion, create `.planning/phases/02-desired-state-discovery-validation/02-03-SUMMARY.md`
</output>
