---
phase: 02-desired-state-discovery-validation
plan: 04
type: execute
wave: 3
depends_on:
  - 02-02
  - 02-03
files_modified:
  - src/unraid_actuator/cli.py
  - src/unraid_actuator/validate.py
  - tests/unit/test_validate_service.py
  - tests/unit/test_validate_cli.py
autonomous: true
requirements:
  - VAL-01
  - VAL-02
  - VAL-07
must_haves:
  truths:
    - "Operator can validate the full configured host tree."
    - "Operator can validate a single app/environment only when both selectors are provided together."
    - "The command exits 0 for warnings-only runs and 1 for any hard validation error."
  artifacts:
    - path: src/unraid_actuator/validate.py
      provides: "End-to-end validation orchestration from active config through report assembly"
      contains: "run_validate"
    - path: src/unraid_actuator/cli.py
      provides: "Thin `validate` subcommand wiring and exit-code behavior"
      contains: "validate"
    - path: tests/unit/test_validate_cli.py
      provides: "Proof of paired scope flags, exit codes, and grouped output"
      contains: "test_validate_requires_both_scope_flags"
  key_links:
    - from: src/unraid_actuator/cli.py
      to: src/unraid_actuator/validate.py
      via: "Parsed `validate` arguments routed into the service with injected runner/config path"
      pattern: "run_validate"
    - from: src/unraid_actuator/validate.py
      to: src/unraid_actuator/discovery.py
      via: "Host-root discovery feeds rule evaluation and Compose preflight"
      pattern: "discover_host_tree"
    - from: src/unraid_actuator/validate.py
      to: src/unraid_actuator/report.py
      via: "Service returns a report that the CLI renders once before choosing exit code"
      pattern: "render_validation_report"
---

<objective>
Wire the complete Phase 2 validation flow into the packaged CLI so operators can validate full-host or scoped desired state before any build or deploy work.

Purpose: Deliver the actual operator-facing `validate` command on top of the contracts, rules, adapters, and renderer created in earlier plans.
Output: Validation orchestration service, CLI subcommand, and end-to-end unit tests for scope handling, schema errors, and exit-code policy.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-desired-state-discovery-validation/02-CONTEXT.md
@.planning/phases/02-desired-state-discovery-validation/02-RESEARCH.md
@src/unraid_actuator/cli.py
@src/unraid_actuator/config.py
@src/unraid_actuator/discovery.py
@src/unraid_actuator/validation_rules.py
@src/unraid_actuator/compose_validation.py
@src/unraid_actuator/report.py

<interfaces>
Use these service interfaces from prior plans:
```python
def load_active_config(*, path: Path = ACTIVE_CONFIG_PATH) -> ActiveConfig: ...
def load_apps_yaml(path: Path) -> tuple[DeclaredEnvironment, ...]: ...
def validate_secret_env_structure(path: Path) -> None: ...
def discover_host_tree(host_root: Path, declared: tuple[DeclaredEnvironment, ...]) -> tuple[DiscoveredEnvironment, ...]: ...
def find_missing_declared_environments(host_root: Path, declared: tuple[DeclaredEnvironment, ...]) -> tuple[DeclaredEnvironment, ...]: ...
def compose_project_name(app: str, environment: str) -> str: ...
def validate_static_compose(candidate: DiscoveredEnvironment, project_name: str, *, runner: CommandRunner) -> tuple[ValidationFinding, ...]: ...
def validate_dynamic_build(candidate: DiscoveredEnvironment, project_name: str, *, runner: CommandRunner) -> tuple[ValidationFinding, ...]: ...
def render_validation_report(report: ValidationReport, *, hostname: str) -> str: ...
```

Keep the CLI thin:
```python
def main(
    argv: Sequence[str] | None = None,
    *,
    runner: CommandRunner | None = None,
    config_path: Path = ACTIVE_CONFIG_PATH,
) -> int: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Orchestrate full-host and scoped validation in one service</name>
  <files>src/unraid_actuator/validate.py, tests/unit/test_validate_service.py</files>
  <read_first>
    - .planning/phases/02-desired-state-discovery-validation/02-CONTEXT.md (D-01 through D-07)
    - .planning/phases/02-desired-state-discovery-validation/02-RESEARCH.md (Recommended Validation Flow, Phase Requirements map)
    - src/unraid_actuator/config.py
    - src/unraid_actuator/schemas.py
    - src/unraid_actuator/discovery.py
    - src/unraid_actuator/validation_rules.py
    - src/unraid_actuator/compose_validation.py
    - src/unraid_actuator/report.py
  </read_first>
  <behavior>
    - Test 1: `run_validate()` validates every declared/discovered environment for `source_path / hostname`, aggregates all hard errors for the run, and still retains undeclared warning findings.
    - Test 2: `run_validate(app="x", environment="y")` still computes naming validity and collision context against the full declared/discovered host set, but restricts returned target findings and Compose preflight execution to the selected target while still checking host-root contracts (`apps.yaml`, active config, `secret-env.ejson`).
    - Test 3: malformed active config or malformed `apps.yaml` surfaces the original `YAMLValidationError` path/message instead of being rewritten into a generic failure.
  </behavior>
  <action>Create `src/unraid_actuator/validate.py` with `run_validate(*, runner: CommandRunner, config_path: Path = ACTIVE_CONFIG_PATH, app: str | None = None, environment: str | None = None) -> ValidationReport`. Load the active config through `load_active_config`, resolve `host_root = config.source_path / config.hostname`, parse `apps.yaml`, and run `validate_secret_env_structure(host_root / "secret-env.ejson")` per D-01/D-02 before deeper work. Discover the host tree, compute missing declared environments, and evaluate pure validation rules over the full declared/discovered host set first so naming validity and `PROJECT_NAME_COLLISION` context for D-03/D-04 and VAL-08 are computed against every host target before any scope filter is applied. For scoped validation (VAL-02), require the exact requested app/environment pair, do not guess partial scope, and do not silently widen the selection; after full-set rule evaluation, restrict returned target-level findings, `checked_targets`, and Compose preflight execution to the selected target only, while still preserving host-root contract failures. For candidates that survive pure rule checks with `SourceKind.COMPOSE` or `SourceKind.BUILD_PY`, derive the project name with `compose_project_name(...)` and invoke the matching Compose preflight helper only for the full run target set or the selected scoped target. Collect every finding for the run per D-06 and return one `ValidationReport` with `checked_targets` reflecting either the full declared host set or the single selected target. Add `tests/unit/test_validate_service.py` that builds temporary host trees, uses `RecordingRunner`, and proves full-host aggregation, scoped-only reporting/preflight behavior, full-host naming collision detection for scoped runs, undeclared warning retention, and schema-error propagation.</action>
  <acceptance_criteria>
    - `grep -q "def run_validate" src/unraid_actuator/validate.py`
    - `grep -q "load_active_config" src/unraid_actuator/validate.py`
    - `uv run pytest tests/unit/test_validate_service.py -q` passes
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/unit/test_validate_service.py -q</automated>
  </verify>
  <done>The project has one reusable validation service that loads the active host config, enforces the Phase 2 boundary, aggregates findings, and supports full-host or selected-target validation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the `validate` subcommand, paired scope flags, and exit-code/report behavior</name>
  <files>src/unraid_actuator/cli.py, tests/unit/test_validate_cli.py</files>
  <read_first>
    - .planning/phases/02-desired-state-discovery-validation/02-CONTEXT.md (D-05, D-06, D-07)
    - src/unraid_actuator/cli.py
    - src/unraid_actuator/validate.py
    - src/unraid_actuator/report.py
    - tests/unit/test_init_command.py
  </read_first>
  <behavior>
    - Test 1: `unraid-actuator validate --app foo` or `--environment prod` alone exits through `argparse` error because both selectors are required together.
    - Test 2: warnings-only validation prints the grouped report and returns exit code `0`.
    - Test 3: any hard validation error prints the grouped report or schema error and returns exit code `1`.
  </behavior>
  <action>Update `src/unraid_actuator/cli.py` to add a `validate` subcommand while preserving the thin argparse shell pattern from Phase 1. Add `--app` and `--environment` as optional scoped selectors, then enforce the pair rule before dispatch: if exactly one is supplied, call `parser.error("--app and --environment must be provided together")`. Route the command to `run_validate(...)` using the existing injected `runner`/`config_path` surface and `selected_runner = runner or _resolve_runner(args.dry_run)`. After the service returns, render once with `render_validation_report(report, hostname=loaded_hostname)` and `print(...)`, then return `1` when `report.has_errors` is true and `0` otherwise so D-05/D-06 hold. Catch `YAMLValidationError` from malformed active config or `apps.yaml`, print the original message to stderr, and return `1` so VAL-07 is operator-visible at the CLI boundary. Add `tests/unit/test_validate_cli.py` covering the paired-scope parser rule, warnings-only exit `0`, hard-error exit `1`, and schema-error handling.</action>
  <acceptance_criteria>
    - `grep -q "\"validate\"" src/unraid_actuator/cli.py`
    - `grep -q -- "--app and --environment must be provided together" src/unraid_actuator/cli.py`
    - `uv run pytest tests/unit/test_validate_cli.py -q` passes
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/unit/test_validate_cli.py -q</automated>
  </verify>
  <done>The packaged CLI exposes `validate`, rejects partial scope flags, prints one grouped report, and exits 0 for warnings-only or 1 for any hard validation error/schema failure.</done>
</task>

</tasks>

<verification>
Run `uv run pytest tests/unit/test_validate_service.py tests/unit/test_validate_cli.py -q`, then run `uv run python -m unraid_actuator validate --help` to confirm the public surface exposes `--app` and `--environment` without changing the Phase 1 init contract.
</verification>

<success_criteria>
- `unraid-actuator validate` exists and works against the active configured host.
- The command supports either full-host validation or one exact app/environment pair.
- The CLI returns exit code `0` for warnings-only runs and `1` for hard errors or schema failures.
</success_criteria>

<output>
After completion, create `.planning/phases/02-desired-state-discovery-validation/02-04-SUMMARY.md`
</output>
