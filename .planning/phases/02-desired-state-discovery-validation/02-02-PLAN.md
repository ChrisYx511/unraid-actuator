---
phase: 02-desired-state-discovery-validation
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/unraid_actuator/discovery.py
  - src/unraid_actuator/validation_rules.py
  - tests/unit/test_discovery.py
  - tests/unit/test_validation_rules.py
autonomous: true
requirements:
  - VAL-03
  - VAL-04
  - VAL-05
  - VAL-08
must_haves:
  truths:
    - "Declared app/environments missing from disk or structurally invalid become hard errors."
    - "Undeclared invalid app/environments are reported as warnings without failing the run."
    - "Compose project naming is derived from canonical `{app}-{environment}` slugs and fails on unsafe or colliding inputs."
  artifacts:
    - path: src/unraid_actuator/discovery.py
      provides: "Host filesystem discovery and source classification for app/environment directories"
      contains: "discover_host_tree"
    - path: src/unraid_actuator/validation_rules.py
      provides: "Severity policy, XOR enforcement, declared-vs-undeclared handling, and compose project name checks"
      contains: "compose_project_name"
    - path: tests/unit/test_validation_rules.py
      provides: "Proof that declared errors, undeclared warnings, and naming collisions are differentiated correctly"
      contains: "test_invalid_or_ambiguous_project_names_fail"
  key_links:
    - from: src/unraid_actuator/discovery.py
      to: src/unraid_actuator/validation_rules.py
      via: "Typed `DiscoveredEnvironment` inputs feed rule evaluation"
      pattern: "source_kind"
    - from: src/unraid_actuator/validation_rules.py
      to: src/unraid_actuator/validation_models.py
      via: "Rule evaluation emits `ValidationFinding` instances with exact severity"
      pattern: "FindingSeverity"
---

<objective>
Implement host-tree discovery and pure rule evaluation so the validator can classify declared and undeclared environments before any Compose subprocess calls.

Purpose: Separate filesystem facts from policy decisions, making severity, XOR enforcement, and naming validation fully unit-testable.
Output: Discovery helpers, naming/severity rule helpers, and unit coverage for missing/invalid/colliding environment states.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-desired-state-discovery-validation/02-CONTEXT.md
@.planning/phases/02-desired-state-discovery-validation/02-RESEARCH.md
@src/unraid_actuator/validation_models.py
@src/unraid_actuator/schemas.py

<interfaces>
From Plan `02-01`:
```python
@dataclass(frozen=True)
class DeclaredEnvironment:
    app: str
    environment: str

@dataclass(frozen=True)
class DiscoveredEnvironment:
    app: str
    environment: str
    path: Path
    declared: bool
    source_kind: SourceKind
    compose_files: tuple[Path, ...]
    build_file: Path | None

@dataclass(frozen=True)
class ValidationFinding:
    severity: FindingSeverity
    code: str
    message: str
    app: str | None = None
    environment: str | None = None
    path: Path | None = None
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Discover host app/environment directories and classify source shape</name>
  <files>src/unraid_actuator/discovery.py, tests/unit/test_discovery.py</files>
  <read_first>
    - .planning/phases/02-desired-state-discovery-validation/02-CONTEXT.md (D-06)
    - .planning/phases/02-desired-state-discovery-validation/02-RESEARCH.md (Recommended Validation Flow steps 5-6, Pitfall 5)
    - src/unraid_actuator/validation_models.py
    - src/unraid_actuator/schemas.py
  </read_first>
  <behavior>
    - Test 1: discovery walks `host_root/{app}/{environment}` and returns one `DiscoveredEnvironment` per environment directory with correct `declared` status.
    - Test 2: source classification returns `SourceKind.COMPOSE` for exactly one Compose file, `SourceKind.BUILD_PY` for only `build.py`, `SourceKind.MISSING` for no source file, and `SourceKind.AMBIGUOUS` for both Compose variants and/or `build.py`.
    - Test 3: declared environments present in `apps.yaml` but absent on disk are surfaced separately so later rules can emit hard errors.
  </behavior>
  <action>Create `src/unraid_actuator/discovery.py` with a `discover_host_tree(host_root: Path, declared: tuple[DeclaredEnvironment, ...]) -> tuple[DiscoveredEnvironment, ...]` helper plus `find_missing_declared_environments(...) -> tuple[DeclaredEnvironment, ...]`. Scan exactly two directory levels under the configured host root (`app/environment`), ignore non-directories, and classify source files by looking only for `docker-compose.yml`, `docker-compose.yaml`, and `build.py`. Treat `docker-compose.yml` and `docker-compose.yaml` as the same source family for XOR purposes: if both are present, or any Compose file appears alongside `build.py`, mark the candidate `AMBIGUOUS` instead of guessing. Add `tests/unit/test_discovery.py` that builds temporary host trees and verifies declared status, missing-declared detection, and ambiguous-source classification.</action>
  <acceptance_criteria>
    - `grep -q "discover_host_tree" src/unraid_actuator/discovery.py`
    - `grep -q "SourceKind.AMBIGUOUS" tests/unit/test_discovery.py`
    - `uv run pytest tests/unit/test_discovery.py -q` passes
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/unit/test_discovery.py -q</automated>
  </verify>
  <done>The validator can now enumerate all host environments, distinguish declared from undeclared targets, and classify source ambiguity before policy checks run.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce declaration severity, XOR failures, and compose naming rules</name>
  <files>src/unraid_actuator/validation_rules.py, tests/unit/test_validation_rules.py</files>
  <read_first>
    - .planning/phases/02-desired-state-discovery-validation/02-CONTEXT.md (D-03, D-04, D-05, D-06)
    - .planning/phases/02-desired-state-discovery-validation/02-RESEARCH.md (Pattern 2, Pitfall 1, Pitfall 2)
    - src/unraid_actuator/validation_models.py
    - src/unraid_actuator/discovery.py
  </read_first>
  <behavior>
    - Test 1: declared missing environments and declared invalid source shapes emit `error` findings, not warnings.
    - Test 2: undeclared invalid environments emit `warning` findings and do not create hard errors by themselves.
    - Test 3: `compose_project_name(app, environment)` accepts only canonical slug inputs matching `^[a-z0-9]+(?:-[a-z0-9]+)*$` and rule evaluation fails on collisions between normalized `{app}-{environment}` names.
  </behavior>
  <action>Create `src/unraid_actuator/validation_rules.py` with pure rule helpers only. Implement `compose_project_name(app: str, environment: str) -> str` using the exact canonical slug regex from research, and raise/emit findings for any name that is unsafe or would collide after combining into `{app}-{environment}` per D-03/D-04. Add rule functions that turn `DiscoveredEnvironment` and missing-declared data into `ValidationFinding` objects with stable codes such as `DECLARED_MISSING`, `SOURCE_XOR`, `SOURCE_MISSING`, `INVALID_PROJECT_NAME`, and `PROJECT_NAME_COLLISION`. Severity policy must follow D-05/D-06 exactly: declared invalid/missing targets are errors, undeclared invalid targets are warnings, and the helpers must return all findings for the current input instead of failing fast. Add `tests/unit/test_validation_rules.py` covering declared failures, undeclared warnings, invalid names, and project-name collisions across multiple targets.</action>
  <acceptance_criteria>
    - `grep -q "def compose_project_name" src/unraid_actuator/validation_rules.py`
    - `grep -q "PROJECT_NAME_COLLISION" src/unraid_actuator/validation_rules.py`
    - `uv run pytest tests/unit/test_validation_rules.py -q` passes
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/unit/test_validation_rules.py -q</automated>
  </verify>
  <done>Declared-vs-undeclared severity, XOR enforcement, and compose project naming are implemented as pure reusable rules with full unit coverage.</done>
</task>

</tasks>

<verification>
Run `uv run pytest tests/unit/test_discovery.py tests/unit/test_validation_rules.py -q` and confirm the output covers both declared hard failures and undeclared warning-only behavior.
</verification>

<success_criteria>
- Host discovery returns typed environment candidates for the configured host tree.
- Missing declared targets, ambiguous sources, and invalid compose naming inputs produce hard errors.
- Undeclared invalid targets produce warnings only.
</success_criteria>

<output>
After completion, create `.planning/phases/02-desired-state-discovery-validation/02-02-SUMMARY.md`
</output>
