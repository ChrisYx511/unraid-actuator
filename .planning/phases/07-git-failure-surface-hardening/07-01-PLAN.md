---
phase: 07-git-failure-surface-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/unraid_actuator/cli.py
  - tests/unit/test_init_command.py
  - tests/unit/test_reconcile_cli.py
  - .planning/ROADMAP.md
  - .planning/PROJECT.md
  - .planning/v1.0-MILESTONE-AUDIT.md
autonomous: true
requirements:
  - GAP-02
must_haves:
  truths:
    - "`init` git failures still surface by re-raising the original `RuntimeError` through the CLI entrypoint."
    - "`reconcile` git failures still surface by re-raising the original `RuntimeError`, while existing `ValueError` and `YAMLValidationError` CLI handling stays intact."
    - "Phase 7 planning and audit docs no longer describe git `RuntimeError` bubbling as a blocker or as work that still needs CLI normalization."
  artifacts:
    - path: tests/unit/test_init_command.py
      provides: "CLI regression coverage for intentional `init` RuntimeError bubbling"
      contains: "RuntimeError"
    - path: tests/unit/test_reconcile_cli.py
      provides: "CLI regression coverage distinguishing bubbled `RuntimeError` from handled validation/value failures"
      contains: "RuntimeError"
    - path: .planning/ROADMAP.md
      provides: "Phase 7 scope rewritten around preserving exception bubbling"
      contains: "bubble"
    - path: .planning/v1.0-MILESTONE-AUDIT.md
      provides: "Audit narrative aligned to the user-approved bubbling policy"
      contains: "bubble"
  key_links:
    - from: src/unraid_actuator/cli.py
      to: src/unraid_actuator/init.py
      via: "`main()` must continue delegating to `run_init(...)` without adding a broad `RuntimeError` catch"
      pattern: "run_init"
    - from: src/unraid_actuator/cli.py
      to: src/unraid_actuator/reconcile.py
      via: "`main()` must continue delegating to `run_reconcile(...)` while only handling existing schema/value failures"
      pattern: "run_reconcile"
    - from: .planning/ROADMAP.md
      to: .planning/PROJECT.md
      via: "Phase 7 wording must consistently describe intentional bubbling plus regression coverage"
      pattern: "bubble"
---

<objective>
Preserve the current git-failure behavior intentionally: git-related `RuntimeError` exceptions from `init` and `reconcile` must keep bubbling, and the docs/tests must say so explicitly.

Purpose: Close the stale audit/documentation gap without adding new CLI error formatting or changing the shipped runtime behavior.
Output: Regression coverage for the CLI exception boundary plus aligned roadmap/project/audit language for Phase 7.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/07-git-failure-surface-hardening/07-CONTEXT.md
@.planning/phases/07-git-failure-surface-hardening/07-DISCUSSION-LOG.md
@src/unraid_actuator/cli.py
@src/unraid_actuator/init.py
@src/unraid_actuator/reconcile.py
@src/unraid_actuator/reconcile_git.py
@tests/unit/test_init_command.py
@tests/unit/test_reconcile_cli.py

<interfaces>
Current CLI/service boundaries to preserve per D-01:
```python
def main(
    argv: Sequence[str] | None = None,
    *,
    runner: CommandRunner | None = None,
    config_path: Path = ACTIVE_CONFIG_PATH,
) -> int: ...

def run_init(
    config: ActiveConfig,
    *,
    runner: CommandRunner,
    dry_run: bool,
    config_path: Path = ACTIVE_CONFIG_PATH,
) -> InitResult: ...

def run_reconcile(
    *,
    runner: CommandRunner,
    config_path: Path = ACTIVE_CONFIG_PATH,
    dry_run: bool = False,
) -> ReconcileResult: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock intentional git RuntimeError bubbling at the CLI boundary</name>
  <files>src/unraid_actuator/cli.py, tests/unit/test_init_command.py, tests/unit/test_reconcile_cli.py</files>
  <behavior>
    - Test 1: Per D-01 and D-03, `main(["init", ...])` re-raises the original git `RuntimeError` from `run_init(...)`/clone failure instead of converting it into exit code `1` stderr output.
    - Test 2: Per D-01 and D-03, `main(["reconcile"])` re-raises the original git `RuntimeError` from `run_reconcile(...)` while the existing `ValueError` path still returns `1` and prints the original message.
    - Test 3: Existing reconcile `YAMLValidationError` handling still returns `1` and prints the original message, and the current success/dry-run dispatch behavior remains unchanged after the new unhappy-path regression coverage is added.
  </behavior>
  <action>Add regression tests first in `tests/unit/test_init_command.py` and `tests/unit/test_reconcile_cli.py` that prove the current policy from D-01 and D-03: git-related `RuntimeError` failures from init/reconcile are supposed to bubble. In the same reconcile CLI test file, keep explicit regression coverage for the existing handled `ValueError` and `YAMLValidationError` paths so execution cannot accidentally broaden bubbling or break the non-git failure surface. If those tests expose accidental drift, make the minimum `src/unraid_actuator/cli.py` change needed to restore the current behavior. Do not add broad `RuntimeError` catches, do not print/suppress bubbled git failures, do not normalize them into exit-code-1 handling, and do not alter existing `YAMLValidationError` or `ValueError` handling for non-git failures per D-04.</action>
  <verify>
    <automated>uv run pytest tests/unit/test_init_command.py tests/unit/test_reconcile_cli.py -q</automated>
  </verify>
  <done>`init` and `reconcile` have explicit CLI regressions proving git `RuntimeError` bubbling is intentional, and any needed code change is limited to preserving that exact behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Realign Phase 7 docs and audit language to the approved bubbling policy</name>
  <files>.planning/ROADMAP.md, .planning/PROJECT.md, .planning/v1.0-MILESTONE-AUDIT.md</files>
  <action>Update only the stale Phase 7 wording called out in D-02. Rewrite the Phase 7 roadmap goal, success criteria, and plan summary so they describe preserving exception bubbling plus regression coverage rather than CLI normalization. Update `PROJECT.md` so Phase 7 is no longer framed as needing clean operator-facing git error formatting. Update the milestone audit so the prior assumption is recorded as stale and the remaining work is documentation/test alignment around intentional bubbling. Keep scope narrow per D-04: do not introduce new requirements, do not perform generic RuntimeError cleanup, do not refactor git helpers, and do not rewrite unrelated phase history.</action>
  <verify>
    <automated>uv run python -c "from pathlib import Path; roadmap=Path('.planning/ROADMAP.md').read_text(); project=Path('.planning/PROJECT.md').read_text(); audit=Path('.planning/v1.0-MILESTONE-AUDIT.md').read_text(); assert 'bubble' in roadmap.lower(); assert 'bubble' in project.lower(); assert 'bubble' in audit.lower(); text='\\n'.join([roadmap, project, audit]).lower(); banned=['normalize git failure handling','clean operator-facing cli','uncaught tracebacks','not yet normalized at the cli boundary','runtime error handling not normalized','git failure cli normalization']; assert all(item not in text for item in banned); print('ok')"</automated>
  </verify>
  <done>Roadmap, project, and milestone-audit language all match the user-approved policy that git `RuntimeError` exceptions intentionally bubble and are stabilized by regression tests instead of CLI normalization.</done>
</task>

</tasks>

<verification>
Run `uv run pytest tests/unit/test_init_command.py tests/unit/test_reconcile_cli.py -q` and the Phase 7 doc-alignment assertion command before finalizing. Treat any change that adds broad `RuntimeError` handling or operator-facing reformatting as a failure against D-01/D-04.
</verification>

<success_criteria>
- CLI regression tests explicitly prove `init` and `reconcile` git `RuntimeError` failures bubble unchanged.
- Existing handled validation/value failures still return exit code `1` with stderr output.
- Phase 7 roadmap/project/audit docs stop describing bubbled git exceptions as a blocker or pending CLI-normalization work.
- No new generic error-handling behavior is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/07-git-failure-surface-hardening/07-01-SUMMARY.md`
</output>
