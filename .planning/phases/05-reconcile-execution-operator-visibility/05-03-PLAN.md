---
phase: 05-reconcile-execution-operator-visibility
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
  - 05-02
files_modified:
  - src/unraid_actuator/build_paths.py
  - src/unraid_actuator/build.py
  - src/unraid_actuator/validate.py
  - src/unraid_actuator/reconcile.py
  - tests/unit/test_build_service.py
  - tests/unit/test_validate_service.py
  - tests/unit/test_reconcile_service.py
autonomous: true
requirements:
  - REC-01
  - REC-02
  - REC-03
  - REC-04
  - REC-05
  - REC-06
  - OPS-01
  - OPS-02
  - OPS-03
must_haves:
  truths:
    - "Reconcile validates and builds the incoming candidate from an isolated checkout before any runtime or managed-source mutation."
    - "When the candidate is valid, removed targets are torn down first from the current runtime tree (rebuilding that current tree from the known-good managed checkout if needed), then the desired runtime tree is applied with `docker compose up`, and the managed checkout advances only after success."
    - "Dry-run reconcile is explicit and non-mutating for the managed source/runtime tree while still exposing what would happen."
  artifacts:
    - path: src/unraid_actuator/reconcile.py
      provides: "Core reconcile coordinator"
      contains: "run_reconcile"
    - path: src/unraid_actuator/build.py
      provides: "Host-root-aware build reuse for candidate checkouts"
      contains: "run_build_for_host"
    - path: src/unraid_actuator/validate.py
      provides: "Host-root-aware validation reuse for candidate checkouts"
      contains: "run_validate_for_host"
    - path: tests/unit/test_reconcile_service.py
      provides: "Proof of no-op, invalid-candidate, removal-failure, success, and dry-run reconcile flows"
      contains: "test_reconcile_applies_candidate_then_fast_forwards_source"
  key_links:
    - from: src/unraid_actuator/reconcile.py
      to: src/unraid_actuator/reconcile_git.py
      via: "Coordinator must use stored candidate SHA/workspace helpers rather than re-deriving Git state"
      pattern: "prepare_candidate_checkout"
    - from: src/unraid_actuator/reconcile.py
      to: src/unraid_actuator/deploy.py
      via: "Removed-target teardown and desired-tree apply must reuse existing runtime services"
      pattern: "run_deploy"
    - from: src/unraid_actuator/reconcile.py
      to: src/unraid_actuator/reconcile_visibility.py
      via: "Lifecycle/failure events and deploy/teardown command results must all flow through the visibility adapter so per-run logs include compose/apply output"
      pattern: "log_command_result"
---

<objective>
Implement the real reconcile execution flow by reusing candidate validation/build plus the existing deploy/teardown services.

Purpose: Deliver the core Phase 5 outcome without inventing a second build/deploy stack inside reconcile.
Output: Host-root-aware validate/build reuse, reconcile orchestration, dry-run semantics, promotion helpers, and unit coverage for the full safety sequence.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-reconcile-execution-operator-visibility/05-CONTEXT.md
@.planning/phases/05-reconcile-execution-operator-visibility/05-RESEARCH.md
@.planning/phases/05-reconcile-execution-operator-visibility/05-VALIDATION.md
@src/unraid_actuator/build.py
@src/unraid_actuator/build_paths.py
@src/unraid_actuator/validate.py
@src/unraid_actuator/deploy.py
@src/unraid_actuator/reconcile_git.py
@src/unraid_actuator/reconcile_plan.py
@src/unraid_actuator/reconcile_visibility.py
@src/unraid_actuator/reconcile_lock.py

<interfaces>
Existing contracts to reuse directly:
```python
def run_validate(
    *,
    runner: CommandRunner,
    config_path: Path = ACTIVE_CONFIG_PATH,
    app: str | None = None,
    environment: str | None = None,
) -> ValidationReport: ...

def run_build(
    *,
    runner: CommandRunner,
    config_path: Path = ACTIVE_CONFIG_PATH,
    output_root: Path | None = None,
) -> BuildResult: ...

def run_deploy(..., build_root: Path | None = None, app: str | None = None, environment: str | None = None) -> RuntimeActionResult: ...
def run_teardown(..., build_root: Path | None = None, app: str | None = None, environment: str | None = None) -> RuntimeActionResult: ...
```

Downstream coordinator target:
```python
def run_reconcile(
    *,
    runner: CommandRunner,
    config_path: Path = ACTIVE_CONFIG_PATH,
    dry_run: bool = False,
) -> ReconcileResult: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lower validate/build to reusable host-root helpers and shared runtime-root promotion</name>
  <files>src/unraid_actuator/build_paths.py, src/unraid_actuator/build.py, src/unraid_actuator/validate.py, tests/unit/test_build_service.py, tests/unit/test_validate_service.py</files>
  <read_first>
    - .planning/phases/05-reconcile-execution-operator-visibility/05-RESEARCH.md
    - src/unraid_actuator/build.py
    - src/unraid_actuator/validate.py
    - src/unraid_actuator/build_paths.py
    - tests/unit/test_build_service.py
    - tests/unit/test_validate_service.py
  </read_first>
  <behavior>
    - Test 1: `run_validate_for_host(host_root=...)` and `run_build_for_host(host_root=..., output_root=...)` produce the same findings/build outputs as the current config-backed wrappers, so candidate checkouts can reuse the existing logic per REC-02/REC-03.
    - Test 2: the original `run_validate(...)` and `run_build(...)` keep their current public behavior by loading active config and delegating to the new host-root helpers.
    - Test 3: runtime-root promotion uses one shared helper instead of duplicate private swap logic, so reconcile can promote a candidate build root into the default runtime root without re-implementing the build swap behavior.
  </behavior>
  <action>Refactor `src/unraid_actuator/validate.py` and `src/unraid_actuator/build.py` to expose host-root-level helpers that accept explicit `host_root` and keep the existing config-path wrappers as thin delegates. This is required by D-02 and the research guidance: reconcile must validate/build an isolated candidate checkout without mutating the persisted active config or writing temporary config files. While touching build internals, extract the current output-root replacement logic into a shared helper in `src/unraid_actuator/build_paths.py` (for example `promote_runtime_root(stage_root, final_root)`) and update `run_build()` to use it, so reconcile can later promote the candidate build root into the default runtime root with the same atomic replace semantics. Preserve existing dry-run behavior for the standalone `build` command, and keep tests focused on parity between the old wrappers and the new reusable helpers. Do not fork build/validate behavior for reconcile; one implementation path must serve both direct commands and candidate evaluation.</action>
  <acceptance_criteria>
    - `grep -q "def run_build_for_host" src/unraid_actuator/build.py`
    - `grep -q "def run_validate_for_host" src/unraid_actuator/validate.py`
    - `grep -q "def promote_runtime_root" src/unraid_actuator/build_paths.py`
    - `uv run pytest tests/unit/test_build_service.py tests/unit/test_validate_service.py -q` passes
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/unit/test_build_service.py tests/unit/test_validate_service.py -q</automated>
  </verify>
  <done>Candidate evaluation can reuse the real validate/build logic against arbitrary host roots, and runtime-root promotion is available as a shared helper instead of a private build-only implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the reconcile coordinator with explicit dry-run, teardown-first ordering, and post-apply source advance</name>
  <files>src/unraid_actuator/reconcile.py, tests/unit/test_reconcile_service.py</files>
  <read_first>
    - .planning/phases/05-reconcile-execution-operator-visibility/05-CONTEXT.md
    - .planning/phases/05-reconcile-execution-operator-visibility/05-RESEARCH.md
    - src/unraid_actuator/reconcile_models.py
    - src/unraid_actuator/reconcile_git.py
    - src/unraid_actuator/reconcile_plan.py
    - src/unraid_actuator/reconcile_visibility.py
    - src/unraid_actuator/reconcile_lock.py
    - src/unraid_actuator/deploy.py
    - src/unraid_actuator/build.py
    - src/unraid_actuator/validate.py
  </read_first>
  <behavior>
    - Test 1: no-op reconcile acquires the lock, emits start/complete file+syslog events, returns success with no managed-source/runtime mutation, and does not call `notify` per REC-01/D-08/D-11.
    - Test 2: invalid incoming candidate state fails after isolated validate/build attempts, logs the failure per OPS-02/OPS-03, and never computes removals or mutates current runtime/source state per REC-03/D-02.
    - Test 3: valid candidate reconcile builds into a reconcile-only candidate root, tears down removed targets from the current runtime tree first per D-03, rebuilds that current runtime tree from the current known-good managed checkout when removals need it and the existing tree is missing/malformed per D-06, aborts immediately on removal failure per D-04, then applies the desired candidate tree with `run_deploy(...)` per D-05/REC-05.
    - Test 4: after successful apply, reconcile promotes the candidate build root into the default runtime root and fast-forwards the managed source to the exact candidate SHA per REC-06; if promotion or fast-forward fails, source advance does not occur.
    - Test 5: `dry_run=True` is explicit per D-10 — it may inspect Git and build/validate the isolated candidate workspace, but it must skip current-runtime rebuild, teardown/apply, candidate-build promotion, and managed-source fast-forward while returning/logging the planned removals, candidate SHA, and any “would rebuild current runtime tree before removals” outcome.
    - Test 6: reconcile forwards `RuntimeActionResult.command_results` from both teardown and deploy into the visibility adapter so compose stdout/stderr lands in the per-run reconcile log per OPS-03.
  </behavior>
    <action>Create `src/unraid_actuator/reconcile.py` with `run_reconcile(*, runner: CommandRunner, config_path: Path = ACTIVE_CONFIG_PATH, dry_run: bool = False) -> ReconcileResult`. Wrap the full lifecycle in `acquire_reconcile_lock(...)` per D-11 and open a visibility session immediately so start/failure/complete events always land in the run log and syslog. Load active config, inspect the managed checkout via `reconcile_git.py`, and short-circuit no-op success when `current_sha == candidate_sha` per REC-01. Otherwise prepare the isolated candidate checkout/work area per D-02, run `run_validate_for_host(...)` and `run_build_for_host(...)` against that candidate host root using a reconcile-only candidate build root under the default build-root parent, and fail immediately if validation/build is not clean per REC-03. Only after the candidate is trusted, compute removals with `plan_removed_targets(...)`; if removals exist and the current default runtime tree is missing or malformed, rebuild that current runtime tree from the managed known-good checkout before teardown per D-06. Then execute `run_teardown(...)` against the current runtime tree for one removed target at a time, stop on the first failure per D-03/D-04, and forward every returned `RuntimeActionResult.command_results` entry into `visibility.log_command_result(...)` (or equivalent) so compose stdout/stderr lands in the per-run file log per OPS-03. For live reconcile, then call `run_deploy(...)` against the candidate build root for the full desired state per D-05/REC-05, again forwarding every `command_results` entry into the visibility adapter, promote the candidate build root into the default runtime root with the shared build-path helper, and fast-forward the managed checkout to the exact candidate SHA per REC-06. For `dry_run=True`, keep the explicit non-mutating contract from D-10: allow isolated candidate inspection/validation/build and removal planning, and report any D-06 rebuild requirement as a planned step, but do not rebuild the current runtime tree, do not call teardown/apply, do not promote the candidate root into the current runtime root, and do not fast-forward the managed checkout. Keep this module as an orchestrator only; do not re-implement Compose command building, target discovery, or direct Git parsing inside it.</action>
  <acceptance_criteria>
    - `grep -q "def run_reconcile" src/unraid_actuator/reconcile.py`
    - `grep -q "dry_run" src/unraid_actuator/reconcile.py`
    - `grep -q "run_teardown" src/unraid_actuator/reconcile.py`
    - `grep -q "run_deploy" src/unraid_actuator/reconcile.py`
    - `uv run pytest tests/unit/test_reconcile_service.py -q` passes
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/unit/test_reconcile_service.py -q</automated>
  </verify>
  <done>Reconcile now exists as one coordinator that honors Git safety, candidate isolation, teardown-first ordering, dry-run non-mutation, operator visibility, locking, and exact-SHA source advancement.</done>
</task>

</tasks>

<verification>
Run `uv run pytest tests/unit/test_build_service.py tests/unit/test_validate_service.py tests/unit/test_reconcile_service.py -q`.

Manual host verification deferred because live Git/Docker/Unraid behavior is unavailable here: on an Unraid host, test one invalid candidate commit, one candidate with a removed target, one successful applied candidate, and one `reconcile --dry-run`, confirming removed targets tear down before apply, success is based on `docker compose up`, and neither the managed checkout nor the current runtime tree changes during dry-run.
</verification>

<success_criteria>
- Candidate validate/build reuse the real service logic from an isolated checkout.
- Reconcile tears down removals first, applies the candidate runtime tree second, and advances source last.
- Public dry-run semantics are explicit and non-mutating for managed source/runtime state.
</success_criteria>

<output>
After completion, create `.planning/phases/05-reconcile-execution-operator-visibility/05-03-SUMMARY.md`
</output>
