---
phase: 05-reconcile-execution-operator-visibility
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/unraid_actuator/reconcile_visibility.py
  - src/unraid_actuator/reconcile_lock.py
  - tests/unit/test_reconcile_visibility.py
  - tests/unit/test_reconcile_lock.py
autonomous: true
requirements:
  - OPS-01
  - OPS-02
  - OPS-03
must_haves:
  truths:
    - "Every reconcile run gets one timestamped file log under `/var/log/unraid-actuator/`."
    - "Operators get syslog lifecycle/failure events, and applied-success/failure notifications use Unraid `notify` only when available."
    - "Only one reconcile can be active at a time, and overlapping runs fail fast instead of interleaving mutations."
  artifacts:
    - path: src/unraid_actuator/reconcile_visibility.py
      provides: "Per-run file logging plus syslog/notification fan-out"
      contains: "open_reconcile_log"
    - path: src/unraid_actuator/reconcile_lock.py
      provides: "Single-run reconcile lock context manager"
      contains: "acquire_reconcile_lock"
    - path: tests/unit/test_reconcile_visibility.py
      provides: "Proof of logging, syslog, and notify behavior"
      contains: "test_noop_logs_skip_notify"
  key_links:
    - from: src/unraid_actuator/reconcile_visibility.py
      to: src/unraid_actuator/runner.py
      via: "Syslog and notify adapters must stay runner-based for dry-run-friendly tests"
      pattern: "CommandSpec"
    - from: src/unraid_actuator/reconcile_lock.py
      to: src/unraid_actuator/reconcile.py
      via: "Core reconcile execution must wrap the full lifecycle in the same lock helper"
      pattern: "acquire_reconcile_lock"
---

<objective>
Create the operator-visibility and concurrency primitives Phase 5 needs before the reconcile service starts mutating anything.

Purpose: Make D-07 through D-11 executable as reusable adapters instead of burying them inside the eventual coordinator.
Output: Timestamped reconcile log sink, syslog/notify fan-out helpers, single-run lock helper, and unit tests for both seams.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-reconcile-execution-operator-visibility/05-CONTEXT.md
@.planning/phases/05-reconcile-execution-operator-visibility/05-RESEARCH.md
@.planning/phases/05-reconcile-execution-operator-visibility/05-VALIDATION.md
@src/unraid_actuator/runner.py
@src/unraid_actuator/cli.py

<interfaces>
Existing runner contract:
```python
@dataclass(frozen=True)
class CommandSpec:
    argv: tuple[str, ...]
    cwd: Path | None = None
    env: Mapping[str, str] = field(default_factory=dict)
    stdin_text: str | None = None
    inherit_env: bool = True

class CommandRunner(Protocol):
    def run(self, spec: CommandSpec) -> CommandResult: ...
```

Design the visibility API so later plans can do:
```python
with open_reconcile_log(...) as visibility:
    visibility.log_event("reconcile started")
    visibility.log_command_result(result)
    visibility.log_warning("notify missing")
    visibility.log_success(...)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement reconcile file/syslog/notify visibility adapters</name>
  <files>src/unraid_actuator/reconcile_visibility.py, tests/unit/test_reconcile_visibility.py</files>
  <read_first>
    - .planning/phases/05-reconcile-execution-operator-visibility/05-CONTEXT.md
    - .planning/phases/05-reconcile-execution-operator-visibility/05-RESEARCH.md
    - src/unraid_actuator/runner.py
    - src/unraid_actuator/cli.py
    - tests/unit/test_runner.py
  </read_first>
  <behavior>
    - Test 1: opening a reconcile log creates exactly one timestamped file beneath `/var/log/unraid-actuator/` per run per D-09, and command/event writes include stdout/stderr only for safe runtime-facing commands like compose apply/teardown so operator-visible apply output lands in the file per OPS-03 without persisting secret-bearing build/decrypt output.
    - Test 2: lifecycle helpers emit `reconcile started` and `reconcile complete` syslog events per OPS-01, but no-op completion is file/syslog only and does not call Unraid `notify` per D-08.
    - Test 3: applied success and failures fan out to file + syslog + `notify`, while a missing `notify` command becomes a warning-only file/syslog event per D-07 instead of failing reconcile.
  </behavior>
  <action>Create `src/unraid_actuator/reconcile_visibility.py` as a small adapter layer, not a general logging framework. Use stdlib file I/O to create `/var/log/unraid-actuator/` as needed and open one UTF-8 log file per reconcile run with a timestamped filename per D-09. Add helper methods that write structured-ish plain text entries for lifecycle events, warnings, safe status summaries, and selected `CommandResult` details. Persist full stdout/stderr only for safe runtime-facing commands whose output is operator-audit material (notably compose teardown/apply, plus any visibility commands such as `logger`/`notify` if desired). For Git/validation/build/decrypt-related commands, log only high-level metadata such as command identity, exit status, and explicitly authored summary text; do not persist raw stdout/stderr under `/var/log/unraid-actuator/` because those flows may surface secret-bearing material outside the runtime tree. Emit syslog through runner-executed `logger -t unraid-actuator ...` command specs so the seam stays testable. For Unraid notifications, preflight availability with `shutil.which("notify")` (or an equivalent injectable resolver) and only invoke it for applied success and failures; if unavailable, write a warning to file/syslog and continue per D-07. Do not send notifications for no-op reconciles per D-08, and do not introduce Python `logging` configuration or third-party logging packages.</action>
  <acceptance_criteria>
    - `grep -q "def open_reconcile_log" src/unraid_actuator/reconcile_visibility.py`
    - `grep -q 'logger' src/unraid_actuator/reconcile_visibility.py`
    - `grep -q 'notify' src/unraid_actuator/reconcile_visibility.py`
    - `uv run pytest tests/unit/test_reconcile_visibility.py -q` passes
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/unit/test_reconcile_visibility.py -q</automated>
  </verify>
  <done>The repo has one visibility adapter that writes per-run logs with safe operator-audit detail, emits syslog lifecycle/failure events, and treats missing `notify` as warning-only while keeping no-op runs out of the notification channel.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the single-run reconcile lock helper</name>
  <files>src/unraid_actuator/reconcile_lock.py, tests/unit/test_reconcile_lock.py</files>
  <read_first>
    - .planning/phases/05-reconcile-execution-operator-visibility/05-CONTEXT.md
    - .planning/phases/05-reconcile-execution-operator-visibility/05-RESEARCH.md
    - src/unraid_actuator/config.py
  </read_first>
  <behavior>
    - Test 1: acquiring the reconcile lock succeeds once and a concurrent second acquire fails immediately with an operator-facing error per D-11.
    - Test 2: the lock releases automatically after success or exception so later reconciles are not blocked by normal failures.
    - Test 3: dry-run reconcile uses the same lock helper, so overlapping dry-run and live reconcile invocations still serialize safely.
  </behavior>
  <action>Add `src/unraid_actuator/reconcile_lock.py` with a context manager such as `acquire_reconcile_lock(path: Path = Path("/tmp/unraid-actuator/reconcile.lock"))`. Use an OS-backed non-blocking exclusive lock (`fcntl.flock(..., LOCK_EX | LOCK_NB)` on the opened file is preferred on Unraid/Linux) so crashes do not leave a permanent “exists” sentinel behind. Write simple metadata like pid and timestamp into the lock file for troubleshooting if helpful, but the essential behavior is: fail immediately when another reconcile already holds the lock per D-11, and always release on exit. Keep this helper filesystem-local and stdlib-only; do not add daemon processes, polling loops, or retry/backoff logic.</action>
  <acceptance_criteria>
    - `grep -q "def acquire_reconcile_lock" src/unraid_actuator/reconcile_lock.py`
    - `grep -q "flock" src/unraid_actuator/reconcile_lock.py`
    - `uv run pytest tests/unit/test_reconcile_lock.py -q` passes
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/unit/test_reconcile_lock.py -q</automated>
  </verify>
  <done>One reusable lock helper now guards the full reconcile lifecycle and fails fast whenever another reconcile is already active.</done>
</task>

</tasks>

<verification>
Run `uv run pytest tests/unit/test_reconcile_visibility.py tests/unit/test_reconcile_lock.py -q`.

Manual Unraid-host verification deferred here: (1) run one no-op reconcile and confirm syslog plus one file log are written with no Unraid notification, (2) run one applied reconcile and confirm start/complete syslog plus a success notification, (3) temporarily hide the `notify` command and confirm reconcile only logs a warning while still completing, and (4) start one reconcile and immediately start a second to confirm the second exits with the lock error.
</verification>

<success_criteria>
- Reconcile visibility is centralized in one adapter instead of scattered prints.
- Every run has one timestamped file log and explicit syslog lifecycle events.
- Overlapping reconcile runs fail fast before any concurrent mutation can start.
</success_criteria>

<output>
After completion, create `.planning/phases/05-reconcile-execution-operator-visibility/05-02-SUMMARY.md`
</output>
