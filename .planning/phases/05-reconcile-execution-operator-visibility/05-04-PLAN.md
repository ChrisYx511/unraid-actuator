---
phase: 05-reconcile-execution-operator-visibility
plan: 04
type: execute
wave: 3
depends_on:
  - 05-03
files_modified:
  - src/unraid_actuator/cli.py
  - tests/unit/test_reconcile_cli.py
autonomous: true
requirements:
  - REC-01
  - REC-02
  - REC-03
  - REC-05
  - OPS-01
  - OPS-02
  - OPS-03
must_haves:
  truths:
    - "Operator can run `unraid-actuator reconcile` from the public CLI."
    - "Operator can run `unraid-actuator reconcile --dry-run` and get explicit non-mutating reconcile behavior."
    - "CLI keeps the existing thin parser-to-service pattern and returns clear exit codes for reconcile failures."
  artifacts:
    - path: src/unraid_actuator/cli.py
      provides: "Public reconcile subcommand wired to the Phase 5 service"
      contains: "\"reconcile\""
    - path: tests/unit/test_reconcile_cli.py
      provides: "Proof of reconcile dispatch, dry-run routing, and failure exits"
      contains: "test_reconcile_dry_run_passes_flag_without_switching_to_global_dry_run_runner"
  key_links:
    - from: src/unraid_actuator/cli.py
      to: src/unraid_actuator/reconcile.py
      via: "CLI must delegate all reconcile behavior to `run_reconcile(...)` and keep business logic out of argparse"
      pattern: "run_reconcile"
---

<objective>
Expose Phase 5 at the public command line without breaking the thin CLI pattern established in earlier phases.

Purpose: Make reconcile and reconcile dry-run operator-accessible while preserving runner injection and clear exit handling.
Output: `reconcile` CLI wiring, reconcile-specific tests, and final public command-surface coverage.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-reconcile-execution-operator-visibility/05-CONTEXT.md
@.planning/phases/05-reconcile-execution-operator-visibility/05-RESEARCH.md
@.planning/phases/05-reconcile-execution-operator-visibility/05-VALIDATION.md
@src/unraid_actuator/cli.py
@src/unraid_actuator/reconcile.py

<interfaces>
Wire this service boundary directly:
```python
def run_reconcile(
    *,
    runner: CommandRunner,
    config_path: Path = ACTIVE_CONFIG_PATH,
    dry_run: bool = False,
) -> ReconcileResult: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the public `reconcile` command with explicit dry-run dispatch</name>
  <files>src/unraid_actuator/cli.py, tests/unit/test_reconcile_cli.py</files>
  <read_first>
    - .planning/phases/05-reconcile-execution-operator-visibility/05-CONTEXT.md
    - src/unraid_actuator/cli.py
    - src/unraid_actuator/reconcile.py
    - tests/unit/test_deploy_cli.py
    - tests/unit/test_cli_smoke.py
  </read_first>
  <behavior>
    - Test 1: `unraid-actuator reconcile` dispatches directly to `run_reconcile(...)` with the injected runner and config path, returning `0` on success and printing a concise result summary.
    - Test 2: `unraid-actuator --dry-run reconcile` or `unraid-actuator reconcile --dry-run` passes `dry_run=True` to the service per D-10 without silently swapping reconcile onto the existing global `DryRunRunner` path that would suppress needed candidate inspection/build behavior.
    - Test 3: reconcile service/schema/runtime failures print the original message to stderr and return exit code `1`, keeping the CLI thin per the established pattern.
  </behavior>
  <action>Update `src/unraid_actuator/cli.py` to add a `reconcile` subcommand that keeps argparse thin and delegates all real behavior to `run_reconcile(...)`. Because D-10 requires explicit reconcile dry-run semantics rather than accidental composition of existing service dry-run paths, wire reconcile so the CLI passes the selected runner plus a boolean `dry_run=args.dry_run` into the service instead of relying on the generic `_resolve_runner(args.dry_run)` behavior alone. Keep the root-level `--dry-run` flag public and compatible with existing commands, but ensure the reconcile path preserves real candidate inspection/build behavior while still skipping managed-source/runtime mutation as defined by the service. On success, print a concise summary naming the status and candidate SHA if available; exact wording is discretionary. Catch `YAMLValidationError`, `ValueError`, and reconcile-specific runtime failures, print the original message to stderr, and return `1`. Add `tests/unit/test_reconcile_cli.py` first to lock dispatch, dry-run routing, exit codes, and coexistence with the existing `init`, `validate`, `build`, `deploy`, and `teardown` commands.</action>
  <acceptance_criteria>
    - `grep -q 'add_parser("reconcile"' src/unraid_actuator/cli.py`
    - `grep -q "run_reconcile" src/unraid_actuator/cli.py`
    - `uv run pytest tests/unit/test_reconcile_cli.py -q` passes
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/unit/test_reconcile_cli.py -q</automated>
  </verify>
  <done>The public CLI exposes reconcile safely, routes dry-run explicitly into the service contract, and keeps parser code free of reconcile business logic.</done>
</task>

</tasks>

<verification>
Run `uv run pytest tests/unit/test_reconcile_cli.py -q`, then `uv run python -m unraid_actuator --help` and `uv run python -m unraid_actuator reconcile --help` to confirm the new public command surface is visible and consistent.

Manual host verification deferred because Unraid-specific syslog/notify behavior is unavailable here: on an Unraid host, run `unraid-actuator reconcile` and `unraid-actuator --dry-run reconcile` to confirm the CLI exposes the command, the dry-run path performs candidate evaluation without mutating current runtime/source state, and failures still surface the underlying message cleanly.
</verification>

<success_criteria>
- Reconcile is publicly invocable through the CLI.
- Reconcile dry-run is explicit at the service boundary, not accidental runner composition.
- CLI exit and stderr behavior match the rest of the tool.
</success_criteria>

<output>
After completion, create `.planning/phases/05-reconcile-execution-operator-visibility/05-04-SUMMARY.md`
</output>
