---
phase: 05-reconcile-execution-operator-visibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/unraid_actuator/reconcile_models.py
  - src/unraid_actuator/reconcile_git.py
  - src/unraid_actuator/reconcile_plan.py
  - tests/unit/test_reconcile_git.py
  - tests/unit/test_reconcile_plan.py
autonomous: true
requirements:
  - REC-01
  - REC-02
  - REC-04
  - REC-06
must_haves:
  truths:
    - "Reconcile hard-fails before any mutation when the managed checkout is dirty or locally diverged."
    - "Incoming desired state is resolved from a separate candidate checkout/work area instead of the managed source tree."
    - "Removed targets are planned from declared host intent, and any current-runtime teardown inputs are resolved safely only after the D-06 rebuild path is honored when needed."
  artifacts:
    - path: src/unraid_actuator/reconcile_models.py
      provides: "Typed reconcile DTOs shared by later plans"
      contains: "ReconcileResult"
    - path: src/unraid_actuator/reconcile_git.py
      provides: "Managed-checkout safety, candidate SHA resolution, incoming checkout, and post-apply fast-forward helpers"
      contains: "prepare_candidate_checkout"
    - path: src/unraid_actuator/reconcile_plan.py
      provides: "Declared-target removal planning plus safe current-runtime resolution helpers"
      contains: "plan_removed_targets"
  key_links:
    - from: src/unraid_actuator/reconcile_git.py
      to: src/unraid_actuator/init.py
      via: "Candidate checkout helpers must reuse the existing active-config repo/branch contract"
      pattern: "deploy_branch"
    - from: src/unraid_actuator/reconcile_plan.py
      to: src/unraid_actuator/deploy_tree.py
      via: "Removal execution targets must be resolved through the shared runtime-tree helpers after the D-06 current-tree rebuild path is satisfied"
      pattern: "resolve_full_tree_targets"
---

<objective>
Lock the Phase 5 safety contracts first: Git safety checks, isolated candidate checkout/build roots, and declared-target removal planning.

Purpose: Give downstream reconcile execution one explicit contract for D-01 through D-06 before wiring visibility or orchestration.
Output: Reconcile DTOs, Git/incoming-checkout helpers, removal-planning helpers, and unit tests proving safety behavior without live Git/Docker.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-reconcile-execution-operator-visibility/05-CONTEXT.md
@.planning/phases/05-reconcile-execution-operator-visibility/05-RESEARCH.md
@.planning/phases/05-reconcile-execution-operator-visibility/05-VALIDATION.md
@src/unraid_actuator/init.py
@src/unraid_actuator/config.py
@src/unraid_actuator/schemas.py
@src/unraid_actuator/build_paths.py
@src/unraid_actuator/deploy_tree.py
@src/unraid_actuator/deploy_models.py
@src/unraid_actuator/runner.py

<interfaces>
Existing contracts to reuse directly:
```python
@dataclass(frozen=True)
class ActiveConfig:
    repo_url: str
    deploy_branch: str
    hostname: str
    source_path: Path

def load_active_config(*, path: Path = ACTIVE_CONFIG_PATH) -> ActiveConfig: ...
def load_declared_environments(host_root: Path) -> tuple[DeclaredEnvironment, ...]: ...
def resolve_output_root(output_root: Path | None) -> Path: ...
def resolve_full_tree_targets(*, build_root: Path, config_path: Path = ACTIVE_CONFIG_PATH) -> tuple[RuntimeTarget, ...]: ...
```

New contracts this plan should introduce for downstream plans:
```python
@dataclass(frozen=True)
class ManagedCheckoutState:
    current_sha: str
    candidate_sha: str
    source_path: Path
    branch: str

@dataclass(frozen=True)
class CandidateWorkspace:
    checkout_root: Path
    host_root: Path
    build_root: Path
    candidate_sha: str

@dataclass(frozen=True)
class ReconcileResult:
    status: str
    current_sha: str
    candidate_sha: str | None
    dry_run: bool
    removed_targets: tuple[RuntimeTarget, ...]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add managed-checkout safety and isolated candidate checkout helpers</name>
  <files>src/unraid_actuator/reconcile_models.py, src/unraid_actuator/reconcile_git.py, tests/unit/test_reconcile_git.py</files>
  <read_first>
    - .planning/phases/05-reconcile-execution-operator-visibility/05-CONTEXT.md
    - .planning/phases/05-reconcile-execution-operator-visibility/05-RESEARCH.md
    - src/unraid_actuator/init.py
    - src/unraid_actuator/config.py
    - src/unraid_actuator/runner.py
    - tests/unit/test_init_command.py
  </read_first>
  <behavior>
    - Test 1: dirty managed checkouts or local commits that are not an ancestor of the fetched deploy-branch candidate fail immediately per D-01, before any candidate checkout or runtime action is attempted.
    - Test 2: a clean managed checkout resolves `current_sha` and exact `candidate_sha`, reports no-op when they match per REC-01/D-08, and never advances the managed source during inspection.
    - Test 3: candidate evaluation materializes a separate incoming checkout/work area plus reconcile-only candidate build root outside the managed source tree per D-02, and post-apply fast-forward uses the exact candidate SHA per REC-06 rather than a moving branch ref.
  </behavior>
  <action>Create `src/unraid_actuator/reconcile_models.py` first with the typed DTOs later plans will consume (`ManagedCheckoutState`, `CandidateWorkspace`, `ReconcileResult`, and any helper enums you need). Then add `src/unraid_actuator/reconcile_git.py` to own Git safety and candidate workspace setup. Use machine-readable Git surfaces only: `git status --porcelain=v2 --branch` for dirty detection, `git fetch --prune origin <deploy_branch>` to resolve the incoming candidate, `git rev-parse HEAD` / `FETCH_HEAD` for exact SHA capture, and `git merge-base --is-ancestor HEAD FETCH_HEAD` so locally-ahead or diverged managed checkouts hard-fail reconcile per D-01. Add a helper that prepares an isolated incoming checkout under a reconcile-only work area (for example beneath `/tmp/unraid-actuator/incoming`) and checks out the exact candidate SHA in detached mode per D-02; do not inspect or build directly from the managed checkout. Also add a fast-forward helper that advances the managed source only to the stored candidate SHA after later plans report successful apply per REC-06. Keep all external calls runner-based and test with `RecordingRunner`; do not add `git worktree`, automatic resets, or any fallback that tolerates local drift.</action>
  <acceptance_criteria>
    - `grep -q "class ManagedCheckoutState" src/unraid_actuator/reconcile_models.py`
    - `grep -q "def prepare_candidate_checkout" src/unraid_actuator/reconcile_git.py`
    - `grep -q "merge-base" src/unraid_actuator/reconcile_git.py`
    - `uv run pytest tests/unit/test_reconcile_git.py -q` passes
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/unit/test_reconcile_git.py -q</automated>
  </verify>
  <done>Git inspection, no-op detection, isolated candidate checkout creation, and exact-SHA fast-forward helpers exist with tests covering the D-01/D-02/REC-01/REC-06 safety contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Plan removed targets from declared host intent and current marked runtime state</name>
  <files>src/unraid_actuator/reconcile_plan.py, tests/unit/test_reconcile_plan.py</files>
  <read_first>
    - .planning/phases/05-reconcile-execution-operator-visibility/05-CONTEXT.md
    - .planning/phases/05-reconcile-execution-operator-visibility/05-RESEARCH.md
    - src/unraid_actuator/schemas.py
    - src/unraid_actuator/deploy_tree.py
    - src/unraid_actuator/deploy_models.py
    - tests/unit/test_deploy_tree.py
  </read_first>
  <behavior>
    - Test 1: removals are computed from current declared targets minus incoming declared targets, preserving current declaration order per D-03 rather than diffing compose files or runtime-tree directories.
    - Test 2: once removals are needed, the helper returns the declared removal set in current declaration order and signals that current-runtime teardown inputs require rebuild when the current runtime tree is missing, unmarked, or malformed per D-06, rather than treating that state as an immediate terminal failure.
    - Test 3: invalid or untrusted incoming state does not produce a removal list, so reconcile cannot treat a broken candidate as “everything removed” per REC-03/D-04.
  </behavior>
  <action>Add `src/unraid_actuator/reconcile_plan.py` with helpers that (a) diff current vs incoming declared targets using `load_declared_environments(...)`, (b) preserve the current `apps.y[a]ml` order for removals per D-03, and (c) resolve those removals back to concrete `RuntimeTarget`s from a supplied current runtime tree via the Phase 4 trust helpers. If removals are required but the current default runtime tree is missing the marker, lacks a required compose/env target directory, or is otherwise malformed, do not treat that as an immediate terminal reconcile outcome in this planning layer; instead expose an explicit rebuild-required signal/exception/result that downstream reconcile orchestration can use to rebuild the current runtime tree from the managed known-good checkout per D-06 before resolving teardown targets. Make this helper accept explicit current/incoming host roots so later plans can call it only after candidate validation/build succeeds per REC-03. Do not infer removals from rendered Compose diffs, stale build-root listings alone, or invalid incoming host data.</action>
  <acceptance_criteria>
    - `grep -q "def plan_removed_targets" src/unraid_actuator/reconcile_plan.py`
    - `grep -q "load_declared_environments" src/unraid_actuator/reconcile_plan.py`
    - `uv run pytest tests/unit/test_reconcile_plan.py -q` passes
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/unit/test_reconcile_plan.py -q</automated>
  </verify>
  <done>Removed-target planning is declaration-driven and explicitly distinguishes between a true teardown-ready current runtime tree and a D-06 rebuild-required state, so later reconcile code never has to guess removal intent.</done>
</task>

</tasks>

<verification>
Run `uv run pytest tests/unit/test_reconcile_git.py tests/unit/test_reconcile_plan.py -q`.

Manual host check deferred because live Git and Docker host state are unavailable here: on an Unraid host with an initialized source checkout, create one dirty managed checkout and one clean checkout with a remote-only candidate commit, then confirm reconcile refuses the dirty case, reports no-op when SHAs match, and shows removed-target planning only after a valid candidate checkout is prepared.
</verification>

<success_criteria>
- Managed-source safety checks hard-fail on dirty or locally diverged checkouts.
- Candidate checkout/build roots are explicitly separate from the managed source tree.
- Removal planning is declaration-driven and safe against missing or malformed current runtime trees.
</success_criteria>

<output>
After completion, create `.planning/phases/05-reconcile-execution-operator-visibility/05-01-SUMMARY.md`
</output>
