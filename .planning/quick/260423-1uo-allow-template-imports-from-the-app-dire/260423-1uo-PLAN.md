# Quick Task 260423-1uo: Allow template imports from the app directory but never above it

## Goal

Adjust template include resolution so environments can reuse template fragments from their shared app directory, while still forbidding any include path that escapes above the app root.

## Tasks

1. **Widen the template include boundary**
   - Files: `src/unraid_actuator/template_render.py`
   - Action: Change include resolution from “must stay under the environment root” to “must stay under the app root,” derived from the environment directory, without permitting traversal above that boundary.
   - Verify: Shared app-level includes resolve successfully, and upward traversal still raises a clear error.
   - Done when: Template rendering accepts app-shared imports and still blocks escaping the app directory.

2. **Update coverage for the new rule**
   - Files: `tests/unit/test_template_render.py`
   - Action: Replace the old rejecting test with coverage for allowed app-shared imports and add a rejection case for escaping above the app root.
   - Verify: Targeted tests and the existing repo checks stay green.
   - Done when: The tests describe the new app-root contract unambiguously.
