# Quick Task 260423-m68: Remove obsolete secret-key environment-variable references

## Goal

Align the repository docs and bootstrap examples with real EJSON behavior by removing the obsolete secret-key environment-variable guidance and relying on `/opt/ejson/keys` instead.

## Tasks

1. **Remove outdated bootstrap guidance**
   - Files: `scripts/4_actuator-init.sh.template`
   - Action: Delete the obsolete export step and make the script flow rely on copying key material into `/opt/ejson/keys`.
   - Verify: Script remains syntactically valid and still documents the intended Unraid bootstrap sequence.
   - Done when: The bootstrap template no longer instructs users to set a secret-key environment variable.

2. **Update repository documentation**
   - Files: `README.md`, `.planning/phases/03-runtime-build-secret-materialization/03-VALIDATION.md`
   - Action: Clarify that host EJSON key material lives in `/opt/ejson/keys` and remove the stale environment-variable wording from the manual verification note.
   - Verify: The repo no longer documents the obsolete environment-variable flow.
   - Done when: Product docs and historical validation notes consistently describe the keydir-based EJSON setup.
