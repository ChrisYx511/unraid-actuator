# Quick Task 260423-1pv: Remove UV_NO_MODIFY_PATH from the Unraid install script

## Goal

Let the uv installer update PATH normally on the Unraid host by removing the `UV_NO_MODIFY_PATH` override from the boot-time install script.

## Tasks

1. **Update the installer script**
   - Files: `scripts/1_actuator-install.sh`
   - Action: Remove the `UV_NO_MODIFY_PATH=1` environment override from the `curl` and `wget` installer paths so uv can manage PATH as intended on the server.
   - Verify: `bash -n scripts/1_actuator-install.sh`
   - Done when: The script still reads clearly and no longer suppresses uv's PATH changes.

2. **Record the quick task**
   - Files: `.planning/STATE.md`, quick-task summary artifacts
   - Action: Capture the change in the standard GSD quick-task summary and completed-tasks table.
   - Verify: The summary references the implementation commit hash and the working tree is clean.
   - Done when: The repo history includes both the code change and the quick-task artifact commit.
