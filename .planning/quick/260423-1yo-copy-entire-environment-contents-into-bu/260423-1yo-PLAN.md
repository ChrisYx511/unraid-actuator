# Quick Task 260423-1yo: Copy entire environment contents into built output

## Goal

Preserve each source environment directory inside the built runtime tree so Docker Compose files can continue to reference sibling files and nested assets after build, while still producing the actuator-managed `docker-compose.yml` and merged `.env` files used for deployment.

## Tasks

1. **Copy source environment contents into the staging tree**
   - Files: `src/unraid_actuator/build.py`
   - Action: Copy each environment directory into its staging output directory before writing the normalized compose output and materialized `.env`.
   - Verify: Extra files and directories from the source environment are present in the built tree, and generated runtime files still land at `docker-compose.yml` and `.env`.
   - Done when: Built environments preserve source-side auxiliary files without regressing compose or env generation.

2. **Add build coverage for copied environment assets**
   - Files: `tests/unit/test_build_service.py`
   - Action: Extend build tests to assert copied environment files survive into the build output for both static compose and template-backed environments, while generated `docker-compose.yml` and `.env` still override source versions as needed.
   - Verify: Targeted build tests and the existing repo verification stack remain green.
   - Done when: Tests clearly describe both the new copy behavior and the unchanged runtime output contract.
