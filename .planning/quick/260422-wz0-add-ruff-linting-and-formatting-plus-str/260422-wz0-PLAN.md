# Quick Task 260422-wz0: Add ruff linting and formatting plus strict basedpyright and fix practical typing errors

## Goal

Add Ruff lint/format and strict basedpyright to the existing `uv` developer workflow, then resolve the resulting issues where practical so the project has a repeatable static-quality baseline.

## Tasks

1. **Add tooling configuration**
   - Files: `pyproject.toml`, `uv.lock`
   - Action: Add `ruff` and `basedpyright` to the dev dependency group, define repo-local Ruff and basedpyright configuration, and sync the lockfile.
   - Verify: Ruff and basedpyright commands run from `uv run`.
   - Done when: The repository exposes stable lint/format/type-check commands without ad hoc flags.

2. **Fix lint and typing issues**
   - Files: `src/unraid_actuator/**/*.py`, `tests/**/*.py`
   - Action: Run Ruff and strict basedpyright, then make targeted code changes using existing patterns, explicit assertions, and narrow casts where needed.
   - Verify: `uv run ruff check .`, `uv run basedpyright`
   - Done when: Lint and practical strict typing issues are resolved without changing intended behavior.

3. **Document and record the tooling baseline**
   - Files: `README.md`, `.planning/STATE.md`, quick-task summary artifacts
   - Action: Document the developer commands briefly, then record the completed quick task in GSD state and summary files.
   - Verify: README reflects the new workflow and the quick-task artifacts point to the finished commit.
   - Done when: The repo and planning surface both describe the new lint/type-check baseline clearly.
